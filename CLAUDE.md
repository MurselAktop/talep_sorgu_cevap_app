# CLAUDE.md — TŞYS (Talep ve Şikâyet Yönetim Sistemi)

> Kullanıcıya görünen uygulama adı: **TŞYS — Talep ve Şikâyet Yönetim Sistemi**
> Kod / klasör adı: `talep_sorgu_cevap_app`

## Proje özeti
Bu, vatandaşların ve kurum personelinin arıza/talep/şikâyet girdiği bir mobil uygulamadır. Girilen her talep ilgili birime yönlendirilir; o birimin personeli tarafından çözülüp rapor yazılır; birim müdürü raporu onaylar veya reddeder. Onaylanırsa sonuç, talebi açan kişiye bildirim olarak ulaşır; reddedilirse personele geri döner ve talep yeniden ele alınır. Talepler metin içerir; ileride fotoğraf ve video ekleri de desteklenecektir.

## Teknoloji yığını
- **Frontend:** Flutter (mobil uygulama)
- **Backend / Veritabanı / Kimlik / Depolama:** Supabase (Docker üzerinde yerel, `localhost:8000`)
- **Önbellek:** Redis (ileride performans katmanı)
- **Konteynerleştirme:** Docker
- **Versiyon kontrolü:** Git + GitHub

## Roller ve yetkiler
Dört rol vardır. Yetki kuralları veritabanı seviyesinde (Supabase Row Level Security / RLS) uygulanır — Flutter tarafında `if` bloklarıyla değil.

- **Vatandaş:** Talep açar; yalnızca kendi taleplerini ve onların sonuçlarını görür.
- **Personel:** Kendi birimine kayıtlıdır. Herhangi bir birime talep açabilir (örn. İK personeli teknik birime yazıcı arızası bildirir). Kendi birimine gelen talepleri çözer ve rapor yazar. Kendi açtığı talebi kendisi de çözebilir (engellenmez; kaydı sistemde resmi olarak tutulur). Personel, SADECE kendisine (`assigned_to` alanında) atanmış talepleri görebilir. Birimine gelen ama başka bir personele atanmış talepleri GÖREMEZ. (Bu, önceki "şeffaflık" kararından BİLİNÇLİ bir sapmadır, 2026-07-14 tarihinde alındı.)
- **Birim müdürü:** Kendi birimine kayıtlıdır. İki noktada devrededir: (1) Talep birime düştüğünde, talebi **belirli bir personele atar** (`assigned_to`). (2) Atanan personelin yazdığı çözüm raporunu görür; **onaylar** veya **reddeder.** Onaylarsa sonuç, talebi açan kişiye gider. Reddederse personele "reddedildi" bildirimi düşer ve talep yeniden ele alınır. Müdür, kendi biriminin TÜM taleplerini görebilir (atanmış/atanmamış fark etmeksizin) — çünkü atama yapabilmesi için önce görmesi gerekir.
- **Admin:** Tüm sisteme sahiptir; kullanıcı ve birim ekler/çıkarır, her şeyi görür ve yönetir.

- **Kayıt modeli:** Vatandaşlar kendi kendine kayıt olur — `auth.users`'a `INSERT` olduğunda tetiklenen bir veritabanı trigger'ı (`handle_new_user`), `public.users` tablosuna `role = 'vatandas'`, `department_id = null` ile otomatik profil oluşturur. Personel, müdür ve admin hesapları kendi kendine kayıt olamaz; bu hesaplar admin tarafından sonradan oluşturulur veya bir vatandaş hesabı admin tarafından yükseltilir.
  > Not: Admin'in personel/müdür rolü atama/yükseltme işlemini yapacağı arayüz henüz tasarlanmadı, MVP sonrası ele alınacak.

> **Planlanan mimari karar (2026-07-13, birim danışman hocasıyla istişare sonrası netleşti — henüz kod olarak uygulanmadı):**
> - **Vatandaş erişimi (hibrit model):** Vatandaş iki şekilde talep açabilir. (a) Hesap oluşturup (mevcut kayıt/giriş sistemiyle) giriş yaparsa, tüm geçmiş taleplerini hesabından görebilir. (b) Hesap açmak istemezse anonim olarak talep açabilir; bu durumda talebe kısa, insan tarafından okunabilir bir erişim kodu (`access_token`, 8-10 haneli, karışıklık yaratan karakterler hariç) otomatik atanır ve e-posta ile bildirilir. Vatandaş bu kodu, ayrı bir "Sonucu Sorgula" ekranından, giriş yapmadan kullanarak talebinin durumunu her zaman sorgulayabilir.
> - **Personel/Müdür/Admin kaydı (davet kodu modeli):** Kendi kendine kayıt olamazlar. Admin, panelinden belirli bir birime ve role bağlı bir davet kodu (personnel invite code) üretir ve ilgili kişiye iletir. Personel, kayıt ekranında e-posta + kendi seçtiği şifre + ad-soyad + bu davet koduyla kayıt olur; kod geçerliyse hesap, kodun tanımladığı rol/birimle oluşturulur ve kod tekrar kullanılamaz hale gelir.
>
> **Durum (2026-07-14):** Uygulandı — hem veritabanı (personnel_invites tablosu, handle_new_user() dallanması, create_request/get_request_by_token RPC'leri) hem Flutter tarafı (login/register ekranları, admin_invite_screen.dart, citizen_guest_menu_screen.dart, request_create_screen.dart, query_result_screen.dart) tamamlandı. Ayrıntılar için bkz. İlerleme Günlüğü.

## Talebi kim açar, kim çözer
- Talebi hem **vatandaş** hem **personel** açabilir.
- Talep, seçilen kategoriye göre ilgili birime düşer.
- **Atama modeli:** Talep birime düştüğünde, birim müdürü talebi belirli bir personele atar (`assigned_to`). Personel talebi kendi seçerek üstlenmez; atama müdür tarafından yapılır.
- **Görünürlük modeli (2026-07-14'te güncellendi — eski "şeffaflık" kararından kasıtlı sapma):** Personel SADECE kendisine atanmış (`assigned_to = kendisi`) talepleri görebilir, birimin geri kalanını göremez. Müdür, kendi biriminin TÜM taleplerini (atanmış/atanmamış) görebilir — atama yapabilmesi bu görünürlüğe bağlıdır. Admin her şeyi görür. Bir talebi açan kişi (vatandaş/personel), atanmış olsun olmasın, HER ZAMAN kendi açtığı talebi "Taleplerim" ekranından görebilir (bu ayrı bir kural, görünürlük modelinden etkilenmez).
- Personel talepleri ile vatandaş talepleri **farklı etiketlenir** (`requester_type` = `vatandas` / `personel`). Bu, filtreleme ve raporlama içindir.
- **Admin yetkisi:** Admin, herhangi bir talebi koşulsuz olarak güncelleyebilir (durum, atama dahil) — kritik durumlarda sisteme müdahale yetkisi.
- **Açan kişinin düzenleme hakkı:** Talebi açan kişi (vatandaş/personel), talep henüz kimseye atanmamışsa (`assigned_to` boşken) talebin içeriğini düzenleyebilir veya iptal edebilir. Talep atandıktan sonra bu hak kapanır.
- **Frontend gereksinimi:** Kullanıcı, zaten atanmış bir talebi düzenlemeye/iptal etmeye çalışırsa, Flutter arayüzünde şu mesajla bir uyarı gösterilmeli: "Talebiniz ilgili birim personeline atanmıştır, şu an talepte değişiklik yapılamaz." Bu kısıtlama hem RLS'te (güvenlik, zorunlu) hem frontend'de (kullanıcı deneyimi, bilgilendirici) uygulanır.

## Kimlik doğrulama (Auth)
- Kayıt ve giriş **e-posta + şifre** ile yapılır.
- Kayıt sonrası **e-posta doğrulaması** istenir (Supabase'in yerleşik e-posta doğrulaması).
- **SMS doğrulaması kullanılmaz** (ücretli servis gerektirdiği için).

> **Planlanan mimari karar (2026-07-13, henüz uygulanmadı):** Login ekranı "Vatandaş Girişi" / "Personel Girişi" olarak iki ayrı giriş noktası sunacak. İkisi de aynı tek Supabase backend'ine bağlanır (ayrı sunucu yok); giriş başarılı olduktan sonra Flutter, dönen kullanıcının rolünü seçilen giriş noktasıyla karşılaştırır — uyuşmazsa oturumu kapatıp "Bu giriş sadece personel/vatandaş için" gibi bir hata gösterir. Personel Girişi ekranında "Kayıt Ol" bağlantısı, davet kodu isteyen personel kayıt ekranına gider.
>
> **Durum (2026-07-14):** Uygulandı — bkz. `lib/screens/login_screen.dart` ve İlerleme Günlüğü. Ayrıca giriş yapmadan devam edip anonim talep açabilme / kod ile sorgulama seçeneği de (`citizen_guest_menu_screen.dart`) eklendi.

## Veri modeli (tablolar)
- **departments** — birimler. Alanlar: `id`, `name`, `is_active` (Faz 4, default `true` — sil yerine pasifleştirme; pasif birim yeni talep/davet dropdown'larında görünmez ama geçmiş kayıtlarda adı görünmeye devam eder).
- **users** — kullanıcılar. Alanlar: `id`, `email`, `full_name`, `role`, `department_id`, `phone`, `il`, `ilce` (Faz 1), `is_active` (Faz 4, default `true` — sil yerine pasifleştirme, sadece `admin_set_user_active()` RPC'siyle değiştirilebilir). (`role` ve `department_id` yetki mantığının temelidir. Personel ve müdür bir birime bağlıdır; vatandaşın birimi yoktur.)
- **requests** — talepler/şikâyetler. Alanlar: `id`, `title`, `description`, `category`, `status`, `requester_type`, `created_by`, `department_id`, `assigned_to`, `access_token`, `created_at`, `resolved_at` (Faz 5, rapor onaylandığında dolar), `sla_notified_at` (Faz 5, SLA eskalasyon bildiriminin tekrar gönderilmesini önler). (`assigned_to`, müdürün talebi atadığı personelin `id`'sidir; atanmamış talepler için `null` olabilir. `created_by` anonim talepler için `null` olabilir. `access_token`, `generate_access_token()` ile otomatik üretilen, anonim sorgulama için kullanılan kısa/okunabilir koddur.)
- **attachments** — foto/video/belge ekleri. Alanlar: `id`, `request_id`, `file_url`, `media_type` (`image` / `video` / `document`), `created_at`. (Dosyalar Supabase Storage'da, `request-attachments` bucket'ında; tabloda yalnızca adres/meta veri saklanır. `document` = PDF/DOCX.)
- **results** — çözüm raporları. Alanlar: `id`, `request_id`, `report_text`, `resolved_by`, `approval_status`, `approved_by`, `created_at`. (`approval_status` = `beklemede` / `onaylandi` / `reddedildi`; `resolved_by` çözen personel, `approved_by` onaylayan müdür.)
- **notifications** — bildirimler. Alanlar: `id`, `user_id`, `request_id`, `message`, `is_read`, `created_at`.
- **personnel_invites** — personel/müdür/admin davet kodları. Alanlar: `id`, `code`, `department_id`, `role`, `created_by`, `used`, `used_by`, `used_at`, `created_at`. (`code`, `generate_invite_code()` ile otomatik üretilir; kod kullanılınca `used = true` olur ve tekrar kullanılamaz. FK davranışları için bkz. aşağıdaki "personnel_invites Foreign Key Kararları" bölümü.)
- **request_department_reassignments** (Faz 4, YENİ) — müdürün bir talebi başka birime yönlendirme geçmişi. Alanlar: `id`, `request_id`, `old_department_id`, `new_department_id`, `reassigned_by`, `reassigned_at`. Sadece `reassign_request_department()` RPC'si (security definer) yazar; istemciden hiç INSERT/UPDATE/DELETE politikası yok, SELECT sadece admin'e açık. `reassigned_by` FK'i `personnel_invites`'taki `created_by`/`used_by` kararıyla aynı gerekçeyle `ON DELETE SET NULL`.

> Not: Birimlerin (departments) somut listesi henüz belirlenmedi; ileride tanımlanacaktır. Bu, kod yapısını etkilemez; yalnızca veri olarak eklenecektir.

> **Planlanan şema değişiklikleri (2026-07-13, henüz uygulanmadı):**
> - `requests.created_by` alanı nullable olacak (anonim talepler için).
> - `requests` tablosuna yeni bir `access_token` sütunu eklenecek (anonim sorgulama için, kısa/okunabilir kod).
> - Yeni bir `personnel_invites` tablosu eklenecek (kod, hedef birim, hedef rol, kullanıldı mı, kimin oluşturduğu).
>
> **Durum (2026-07-14):** Uygulandı — üç madde de veritabanında tamamlandı (yukarıdaki tablo listesine yansıtıldı). Ayrıca `requests.requester_type` için `in ('vatandas', 'anonim', 'personel')` CHECK constraint'i eklendi.

## personnel_invites Foreign Key Kararları (ON DELETE davranışları)

- department_id → departments.id : ON DELETE CASCADE
  (Birim silinirse, o birime ait kullanılmamış/kullanılmış davet kodları da silinir. Sebep: kodun tek işlevi "şu birime personel ata" demek, birim yoksa kodun sistemde durmasının anlamı yok.)

- created_by → users.id (admin) : ON DELETE SET NULL
  (Admin hesabı silinirse, oluşturduğu davet kodları silinmez, sadece "kim oluşturdu" bilgisi NULL olur. Kodun işlevselliği bundan etkilenmez.)

- used_by → users.id (personel) : ON DELETE SET NULL
  (Personel hesabı silinirse, kullandığı davet kodu kaydı silinmez, sadece "kim kullandı" bilgisi NULL olur. Böylece "kaç kod üretildi, kaçı kullanıldı" gibi istatistikler bozulmaz.)

## Gelecekte Değerlendirilebilir: Soft Delete Deseni

Şu an users tablosunda bir kullanıcı silindiğinde satır fiziksel olarak veritabanından kalkıyor (hard delete). İleride, kurumsal bir yaklaşım olarak users tablosuna is_active (boolean) veya deleted_at (timestamp, nullable) sütunu eklenip "silme" işleminin aslında bu alanı güncellemek olması (soft delete) değerlendirilebilir. Bu sayede personel/admin hesapları hiçbir zaman fiziksel olarak silinmez, geçmiş kayıtlar (davet kodları, talepler, sonuçlar) hep tutarlı kalır. Bu, MVP kapsamının dışında, ileride ele alınacak bir iyileştirme notu olarak buraya eklendi, şu an için herhangi bir kod değişikliği gerektirmiyor.

## Talep durum akışı
`requests.status`: `acik` → `cozuldu (onay bekliyor)` → `onaylandi` (kullanıcıya iletildi) **veya** `reddedildi` (personele geri döndü, yeniden işleme alınır). Reddedilen talep tekrar çözülüp yeniden onaya sunulabilir.

- **Rapor düzenleme:** Personel, çözüm raporunu müdür onaylayana/reddedene kadar (`results.approval_status = 'beklemede'` iken) düzenleyebilir.
- **İptal:** Talebi açan kişi, talebi yalnızca henüz kimse üstlenmemişse (`status = 'acik'` **ve** `assigned_to IS NULL`) iptal edebilir.

## Talep oluşturma ve kategori mantığı
- Kullanıcı bir metin alanına talebini/şikâyetini yazar ve **kategoriyi kendisi seçer.**
- Seçilen kategori, talebin gideceği birimi belirler.
- **MVP'de akış sadedir:** yaz, kategori seç, gönder. Akıllı öneri/uyarı yoktur.

## Gelecek aşama hedefi — Gemini ile talep yorumlama
Çekirdek çalıştıktan sonra eklenecektir (MVP'de **değil**):
- Kullanıcının yazdığı talep metni **Google Gemini API** ile yorumlanır.
- Amaç: (a) yazılan içerik seçilen kategoriyle uyuşmuyorsa kullanıcıya uyarı göstermek (kullanıcı yine de devam edebilir; uygulama zorlamaz), ve/veya (b) içeriğe göre ilgili olabilecek birimleri öneri olarak sıralamak.
- **Güvenlik kuralı:** Gemini API anahtarı **asla Flutter uygulamasına konmaz.** Çağrı, Supabase Edge Function üzerinden yapılır; anahtar orada gizli kalır. Uygulama sadece Supabase'e "bu metni yorumla" der.
- Gemini'nin ücretsiz kotasının sınırlı olduğu göz önünde bulundurulur.

## Bildirim akışı
- Müdür bir raporu **onaylayınca:** talebi açan kişiye "talebiniz sonuçlandı" bildirimi gider.
- Müdür bir raporu **reddedince:** çözen personele "reddedildi, yeniden inceleyin" bildirimi gider.
- (Push bildirim altyapısı ileride kurulur; MVP'de uygulama içi bildirim yeterlidir.)

**Gelecek aşama hedefi:** Bir talebin ataması (`assigned_to`) değiştiğinde (örn. admin ya da müdür tarafından yeniden atama yapıldığında), üç tarafa otomatik e-posta bildirimi gönderilmeli: eski atanmış personel, ilgili birim müdürü, yeni atanan personel. Bu, veritabanı seviyesinde bir trigger (`requests` tablosunda `assigned_to` değiştiğinde tetiklenen) ve bir Supabase Edge Function ile yapılacak (e-posta servisi API anahtarı hiçbir zaman Flutter koduna girmeyecek, Edge Function içinde gizli kalacak). MVP'de değil, RLS ve temel akış tamamlandıktan sonraki bir aşamada ele alınacak.

## RLS Planı
Aşağıdaki kurallar Supabase Row Level Security politikaları olarak tablo bazında uygulanır (nihai hâl).

> **Durum:** Bu RLS planı, Supabase Studio (Authentication > Policies) üzerinden elle, tıklanarak uygulandı ve tamamlandı. Toplam 22 politika, 5 tabloya dağıtıldı ve isim/sayı bazında doğrulandı:
> - departments: 3 politika (1 SELECT, 1 INSERT, 1 UPDATE)
> - users: 5 politika (3 SELECT, 1 INSERT, 1 UPDATE)
> - requests: 7 politika (3 SELECT, 1 INSERT, 3 UPDATE)
> - results: 4 politika (1 SELECT, 1 INSERT, 2 UPDATE)
> - notifications: 3 politika (1 SELECT, 1 INSERT, 1 UPDATE)

**departments:**
- `SELECT`: herkes (public)
- `INSERT`: sadece admin — `(select role from users where id = auth.uid()) = 'admin'`
- `UPDATE`: sadece admin — aynı koşul

**users:**
- `SELECT` (3 ayrı kural): (1) kendi profili — `id = auth.uid()`; (2) müdür kendi biriminin personelini görebilir — `department_id` eşleşmesi + rol = `mudur`; (3) admin herkesi görebilir — rol = `admin`
- `INSERT`: sadece kendi kaydını oluşturabilir — `id = auth.uid()`
- `UPDATE`: kendi profilini günceller ama rol ve `department_id` değişmemiş olmalı (`with check` ile korunur, `is not distinct from` kullanılarak null uyumluluğu sağlanır)

**requests** (8 kural):
- `SELECT` (4 kural, 2026-07-14'te güncellendi): (1) açan kişi kendi talebini görür — `created_by = auth.uid()` (`acan_kisi_kendi_talebini_gorebilir`); (2) müdür kendi biriminin TÜM taleplerini görür — `department_id` eşleşmesi + rol = `mudur` (`mudur_biriminin_tum_taleplerini_gorebilir`); (3) personel SADECE kendisine atanan talepleri görür — `assigned_to = auth.uid()` + rol = `personel` (`personel_atanan_talepleri_gorebilir`); (4) admin hepsini görür (`admin_tum_talepleri_gorebilir`). Not: personel artık biriminin tüm taleplerini göremez, bu eski "şeffaflık" planından bilinçli bir sapmadır.
- `INSERT`: giriş yapmış herkes, `created_by = auth.uid()` olmalı
- `UPDATE` (3 kural): (1) müdür, kendi biriminde durum/atama günceller; (2) açan kişi, sadece `assigned_to` boşken düzenler/iptal eder; (3) admin koşulsuz günceller

**results:** `SELECT` ilgili talebi görebilen herkes; `INSERT` sadece atanan personel; `UPDATE` (içerik) atanan personel sadece `approval_status = 'beklemede'` iken; `UPDATE` (`approval_status`) sadece müdür.

**notifications:** `SELECT` sadece kendine gelen; `INSERT` sistem/trigger otomatik; `UPDATE` (okundu) sadece kendi bildirimi.

> **Güncelleme (2026-07-10):** `users` tablosunda kendi kendine subquery yapan politikalar "infinite recursion detected in policy for relation users" (kod 42P17) hatası verdi. Çözüm olarak `current_user_role()` ve `current_user_department()` adında iki `security definer` fonksiyon oluşturuldu (RLS'i by-pass ederek `users` tablosunu okuyorlar, böylece döngü kırılıyor). `users`, `requests`, `results` tablolarındaki ilgili tüm politikalar (`(select role from users where id = auth.uid())` / `(select department_id from users where id = auth.uid())` kalıbı geçenler) bu fonksiyonları kullanacak şekilde yeniden yazıldı. `notifications` tablosunda bu kalıp hiç kullanılmadığı için dokunulmadı.

> **Güncelleme (2026-07-14):** Hibrit vatandaş erişimi ve davet kodu modeli için RLS genişletildi: (1) `personnel_invites` tablosuna admin-only 4 politika eklendi (`current_user_role() = 'admin'` ile SELECT/INSERT/UPDATE/DELETE); (2) `requests` tablosuna, `anon` rolü için ayrı bir `INSERT` politikası eklendi (`to anon with check (created_by is null)`) — böylece anonim kullanıcılar da (giriş yapmadan) talep açabiliyor. `create_request` ve `get_request_by_token` RPC fonksiyonları `security definer` olarak yazıldığı için PostgREST'in `.insert().select()` ikili SELECT+INSERT RLS gereksinimini RLS politikası eklemeden aşıyor; bu iki fonksiyon kendi içlerinde `auth.uid()` doğrulaması yapıyor.

## Geliştirme prensipleri (Claude Code bunlara uyacak)
- **Parçalı ve modüler kod yaz.** Tek dev dosyalar oluşturma; her şeyi mantıklı klasörlere ve ayrı dosyalara böl (ekranlar, veri modelleri, servisler, Supabase bağlantısı ayrı ayrı).
- **Adım adım ilerle.** Tek seferde çok sayıda dosya üretme; görevi küçük, anlaşılır parçalara böl.
- **Her değişikliği açıkla.** Ne yaptığını ve neden yaptığını kısaca belirt (kullanıcı yeni başlayan bir geliştiricidir, öğrenmek istiyor).
- **Güvenlik önce gelir.** Özellikle RLS ve API anahtarları konusunda dikkatli ol; hassas bilgileri koda gömme.
- **Yetkilendirmeyi RLS ile yap**, uygulama katmanında değil.
- **Türkçe açıklama, standart kod.** Kod içi değişken/fonksiyon adları İngilizce standartta olabilir; açıklamalar ve kullanıcıyla iletişim Türkçe.

## Geliştirme sırası (yol haritası)
1. ✅ Git + GitHub bağlantısı
2. ✅ Flutter iskeleti + Supabase paketi
3. ✅ Kimlik doğrulama (e-posta + şifre + e-posta doğrulaması) ve roller + RLS (SELECT tüm birime açık, UPDATE yalnızca `assigned_to = kendisi` olan personele açık)
4. ✅ Çekirdek veri modeli (tablolar)
5. ✅ Çalışan çekirdek / MVP (talep aç, listele, müdür personele atar, atanan personel çözer, müdür onayı, sonuç) — tüm alt adımlar tamamlandı, bkz. MVP tamamlanma kontrol listesi

## MVP tamamlanma kontrol listesi (madde 5'in alt adımları)
- [x] Talep oluşturma (vatandaş/anonim/personel, create_request RPC)
- [x] Talep listeleme (Gelen Talepler: müdür biriminin tümü, personel sadece atananı; Taleplerim: herkes kendi oluşturduğu; anonim/vatandaş için access_token ile sorgulama)
- [x] Talep detay ekranı
- [x] Talep düzenleme/iptal (açan kişi, SADECE assigned_to boşken; atanmışsa "Talebiniz ilgili birim personeline atanmıştır, şu an talepte değişiklik yapılamaz." uyarısı gösterilmeli)
- [x] Talep atama ekranı (müdür → personel, assigned_to güncellemesi)
- [x] Talep çözümleme ekranı (atanan personel, results tablosuna rapor yazma)
- [x] Onay ekranı (müdür onay/red, red durumunda personele geri dönüş akışı)
- [x] Basit uygulama içi bildirim ekranı (notifications tablosu zaten var, Flutter tarafı yok — CLAUDE.md'nin kendi tanımına göre MVP'de push değil ama in-app bildirim gerekli)

6. ✅ Medya ekleri (foto/video/belge — Supabase Storage; seçme, yükleme, görüntüleme uçtan uca tamamlandı)
7. ✅ Genişletme Faz 1 — Veri modeli + kayıt ekranı genişletmesi (tc_no, telefon, il, ilçe; `users_private` tablosu; KVKK aydınlatma metni + açık rıza hariç — bkz. Production Öncesi Kontrol Listesi)
8. ✅ Genişletme Faz 2 — Hesap yönetimi (Profil sekmesi, şifre değiştirme, şifremi unuttum akışı, telefon otomatik biçimlendirme)
9. ✅ Genişletme Faz 3 — Liste kullanılabilirliği (talep listelerinde arama + filtreleme)
10. ✅ Genişletme Faz 4 — Admin ve müdür yönetim araçları (birim yönetimi, talep yönlendirme, hesap pasifleştirme)
11. ✅ Genişletme Faz 5 — SLA + istatistikler (çözüm süresi, eskalasyon uyarısı, admin/müdür istatistik ekranları)
12. Bildirimler (push) — bilinçli olarak Faz 1-5'in SONRASINA ertelendi (2026-07-20 kararı)
13. Gemini ile talep yorumlama (gelecek aşama)
14. Performans (Redis) ve dağıtım (Docker)

> Faz 1-5'in kapsam detayları için bkz. aşağıdaki "Genişletme Fazları (2026-07-20 planı)" bölümü. Her faz, kullanıcı onayı alınarak teker teker uygulanacak; bir faz bitip test edilmeden sonrakine geçilmeyecek.

## Genişletme Fazları (2026-07-20 planı)

### Faz 1 — Veri modeli + kayıt ekranı genişletmesi ✅ TAMAMLANDI (2026-07-20)
**Alınan kararlar (2026-07-20, kullanıcıyla netleştirildi):**
- `tc_no` görünürlüğü: SADECE kullanıcının kendisi + admin. RLS satır bazlı olduğu ve müdür birim personelinin `users` satırını zaten görebildiği için, `tc_no` `users` tablosuna DEĞİL, ayrı bir `users_private` tablosuna konacak (RLS: kendi satırı + admin).
- Yeni alanların HEPSİ kayıtta zorunlu: `tc_no`, `phone` (telefon numarası — kullanıcı isteğiyle kapsama eklendi), `il`, `ilce`. Zorunluluk form + trigger seviyesinde; DB sütunları mevcut hesaplar null kalabildiği için nullable.
- Kapsam: Hem vatandaş kaydı (`register_screen.dart`) hem davet kodlu personel kaydı (`personnel_register_screen.dart`).
- `tc_no` doğrulaması: DB'de CHECK (11 hane, 0 ile başlamaz) + UNIQUE; Flutter'da ayrıca T.C. kimlik no algoritma kontrolü.
- KVKK bloğu (2026-07-20 onayında kapsamdan ÇIKARILDI): aydınlatma metni, açık rıza kutusu ve `kvkk_accepted_at` sütunu Faz 1'de UYGULANMAYACAK. Uygulama gerçek kullanıcılara açılmadan önce mutlaka eklenecek — bkz. "Yapılacaklar / Production Öncesi Kontrol Listesi".

**Etkilenen yerler:** `users` tablosu (+`phone`, `il`, `ilce`), YENİ `users_private` tablosu (+RLS), `handle_new_user()` trigger'ı (yeni metadata alanlarını okuyup iki tabloya yazacak), migration dosyası, `register_screen.dart`, `personnel_register_screen.dart`, ortak doğrulama dosyası (`lib/utils/validators.dart`).

### Faz 2 — Hesap yönetimi ✅ TAMAMLANDI (2026-07-21)
- Ana ekrana Profil sekmesi: kendi bilgilerini görme + düzenleme (`role` ve `department_id` değiştirilemez — mevcut `users` UPDATE RLS'i zaten koruyor).
- Şifre değiştirme ekranı (giriş yapmışken, `auth.updateUser`).
- Şifremi unuttum akışı (Supabase yerleşik `resetPasswordForEmail`). Yerel ortamda SMTP olmadığı için test yöntemi (örn. yerel bir test SMTP konteyneri) faz başında açıklanacak.
- Telefon numarası alanı otomatik biçimlendirme: Telefon alanı (hem kayıt ekranlarında hem bu fazda eklenecek profil ekranında), kullanıcı yazarken otomatik olarak `+90 (5XX) XXX XX XX` formatında görünecek şekilde biçimlendirilecek — sabit `+90` öneki otomatik eklenir, kullanıcı sadece 10 haneli numarayı (başında 0 olmadan, 5 ile başlayan) girer, uygulama bunu parantez ve boşluklarla gruplandırıp gösterir. Veritabanına hangi formatta kaydedileceğine (sadece rakamlar mı, `+90` dahil mi) faz başında karar verilecek — tutarlılık için tek bir format seçilmeli. Bu sadece görsel/kullanılabilirlik iyileştirmesidir; mevcut telefon doğrulama mantığını (`validators.dart`) bozmayacak şekilde uygulanacak.
- **Etkilenen yerler:** YENİ `profile_screen.dart`, YENİ `change_password_screen.dart`, YENİ `forgot_password_screen.dart`, YENİ `reset_password_screen.dart` (6 haneli kod + yeni şifre), YENİ `lib/utils/phone_input_formatter.dart`, `login_screen.dart` ("Şifremi unuttum" bağlantısı), `home_screen.dart` (AppBar'a Profil ikonu), `register_screen.dart` ve `personnel_register_screen.dart` (telefon alanına biçimlendirme), `lib/services/auth_service.dart` (`updatePassword`, `resetPasswordForEmail`), `lib/utils/validators.dart` (phone/normalizePhone E.164'e geçti, `phoneToDisplay` eklendi). Tablo değişikliği yok (mevcut `users.phone` sütunu, CHECK kısıtlaması yok).

**Şifremi unuttum: link neden kullanılmıyor (2026-07-21 araştırması):** `resetPasswordForEmail` çağrısının gönderdiği e-posta HEM bir link HEM de 6 haneli bir kod içeriyor (`"Alternatively, enter the code: XXXXXX"`). Link, GoTrue'nun `/auth/v1/verify` endpoint'ini tetikleyip token'ı doğruluyor ve `SITE_URL`'e (bu projede `http://localhost:3000` — hiçbir şey çalışmıyor) `#access_token=...&refresh_token=...` URL fragment'iyle 303 yönlendirmesi yapıyor; bu hem uygulamayı hiç açmıyor (custom URL scheme/App Link yok) hem de token'ı hiçbir kodun okumadığı bir yerde bırakıyor — curl ile `-D -` kullanılarak bizzat doğrulandı. Bunun yerine e-postadaki 6 haneli kod kullanılıyor: aynı tek kullanımlık token'ın ikinci bir temsili (biri kullanılınca diğeri de geçersiz oluyor, doğrulandı), `auth.verifyOTP(type: OtpType.recovery, email:, token:)` ile doğrulanınca doğrudan JSON bir oturum (access/refresh token) dönüyor — tarayıcı, deep link veya yeni paket gerekmiyor, `supabase_flutter`'ın zaten sağladığı bir metot. `SITE_URL=http://localhost:3000` sorunu bu yüzden artık ele alınmasına gerek olmayan, ilgisiz bir detay.

`ResetPasswordScreen` tasarım kararı: `verifyOTP` başarılı olduğunda `supabase_flutter` dönen oturumu otomatik olarak geçerli oturum yapıyor; bu yüzden `updatePassword()` için ayrıca giriş gerekmiyor ve başarı sonrası kullanıcı tekrar giriş yapmaya zorlanmadan doğrudan `HomePage`'e yönlendiriliyor (zaten geçerli bir oturumu varken tekrar şifre sormak gereksiz sürtünme olurdu).
- **Kararlar (2026-07-21, kullanıcıyla netleştirildi):** (1) Telefon DB formatı `05XXXXXXXXX`'ten E.164'e (`+905XXXXXXXXX`) geçirildi — DB'de CHECK olmadığı için migration gerekmedi, sadece `validators.dart` yeniden yazıldı. (2) Yerel e-posta testi için Inbucket (self-hosted Supabase'in `dev/docker-compose.dev.yml` override'ı) başlatıldı. (3) Profil ekranında e-posta salt-okunur bırakıldı (düzenleme, Supabase'in e-posta onay akışı + `public.users.email`/`auth.users.email` senkron karmaşıklığı getirdiği için kapsam dışı tutuldu). (4) Şifremi unuttum akışı: deep link YERİNE Supabase'in aynı e-postada gönderdiği 6 haneli alternatif kod kalıcı çözüm olarak benimsendi — bkz. aşağıdaki "Şifremi unuttum: link neden kullanılmıyor" notu.

### Faz 3 — Liste kullanılabilirliği ✅ TAMAMLANDI (2026-07-21)
- Talep listelerine arama (başlık/açıklama metni, `ilike`) ve filtreleme (durum, kategori, tarih aralığı).
- **Etkilenen yerler:** YENİ `lib/widgets/request_filters.dart` (projedeki ilk paylaşılan widget — `RequestFilters` değer sınıfı + `applyTo()` + `RequestFilterSheet` bottom sheet + paylaşılan `statusLabels`), `request_list_screen.dart`, `my_requests_screen.dart` (arama kutusu + filtre ikonu + sorguya `applyTo()`). Şema değişikliği yok; RLS zaten üst sınırı belirliyor, filtreler **server-side** (Supabase sorgusuna `.or()`/`.eq()`/`.ilike()`/`.gte()`/`.lt()` olarak) uygulanıyor — istemci tarafında `.where()` YOK (gerekçe: ekranlar zaten RLS'in izin verdiği kümeyi `department_id`/`assigned_to`/`created_by` ile server-side daraltıyor, arama/filtreyi client-side yapmak aynı listeyi iki farklı katmanda daraltan bir tutarsızlık yaratırdı; ayrıca şikâyet hacmi arşivlenmediği için sınırsız büyüyor).
- Kategori filtresi bilinçli olarak serbest metin `ilike` (dropdown DEĞİL) — `category` DB'de tamamen serbest metin (CHECK/lookup tablosu yok), bir dropdown için PostgREST'te olmayan bir `DISTINCT` sorgusu (yeni view/RPC) gerekirdi.
- `.or()` filtresi için escape: PostgREST'in `.or()` grameri `,`/`(`/`)` karakterlerini ayırıcı olarak kullanıyor ve bunu kendi içinde escape ETMİYOR (postgrest paketi kaynağından doğrulandı) — kullanıcı arama metni `"..."` içine alınıp içindeki `\`/`"` karakterleri escape edilerek gönderiliyor (`request_filters.dart` içindeki `_escapeFilterValue`).
- **Test notu (önemli, ileride benzer bir debug'ı önlemek için):** Bu makinedeki Windows/mingw `curl` derlemesi, `--data-urlencode` ile Türkçe karakter (ör. `ö`, `ğ`) içeren değerleri hatalı encode edip PostgREST'ten `500 Something went wrong` döndürüyor — bu backend'de veya Flutter/Dart tarafında bir hata DEĞİL, sadece bu curl derlemesinin bir test-aracı kusuru. Elle doğru UTF-8 percent-encoding yapılınca (veya gerçek bir Dart `Uri.encodeQueryComponent` çıktısı kullanılınca) aynı istek sorunsuz çalışıyor. Escape/AND/OR/tarih-aralığı mantığının tamamı, gerçek `RequestFilters.applyTo()` kodunun ürettiği string'ler bir Dart scriptiyle üretilip doğru encode edilerek canlı DB'ye karşı doğrulandı.

### Faz 4 — Admin ve müdür yönetim araçları ✅ TAMAMLANDI (2026-07-21)
- Admin birim yönetim ekranı: birim ekleme/düzenleme; silme yerine PASİFLEŞTİRME (`departments.is_active`).
- Talep yönlendirme: müdür, yanlış birime düşen talebi başka birime aktarabilsin (`department_id` güncelleme; müdürün UPDATE RLS'i "kendi birimi" koşullu olduğu için yeni satır değeri politikaya takılır — bu doğrulandı ve `security definer` bir RPC yazıldı: `reassign_request_department()`). Yönlendirme geçmişi ayrı bir tabloda (`request_department_reassignments`) tutuluyor (kullanıcı kararı). Yönlendirme kapsamı sadece `status = 'acik'` olan taleplerle sınırlandırıldı (kullanıcı kararı).
- Hesap pasifleştirme: `users.is_active` (mevcut "Soft Delete Deseni" notuyla birleşti). Yaklaşım: RLS-only (RESTRICTIVE politikalar + `current_user_is_active()` helper), `banned_until`/service_role KULLANILMADI — gerekçe: bu repoda service_role anahtarı hiç yok, Edge Function altyapısı kurulu değil, RLS-only mevcut araçlarla ve "yetkilendirmeyi RLS ile yap" ilkesiyle uyumlu. **Kritik bulgu:** `SECURITY DEFINER` fonksiyonlar RLS'i bypass ettiği için (tablo sahibi postgres, `FORCE ROW LEVEL SECURITY` yok), RESTRICTIVE politikalar `create_request()` gibi RPC'lerin içinde hiç değerlendirilmiyor — bu yüzden her RPC'nin içine AYRICA `current_user_is_active()` kontrolü eklendi.
- **Etkilenen yerler:** `departments` (+`is_active`), `users` (+`is_active`), YENİ `request_department_reassignments` tablosu, YENİ `current_user_is_active()` helper, YENİ `admin_set_user_active()` ve `reassign_request_department()` RPC'leri, `create_request()` güncellemesi (is_active kontrolü), RESTRICTIVE RLS politikaları (users/requests/results/notifications/personnel_invites/attachments), YENİ `admin_department_screen.dart`, YENİ `admin_users_screen.dart`, `request_detail_screen.dart` (Birim Değiştir aksiyonu), `login_screen.dart`/`home_screen.dart` (is_active kontrolü), `request_create_screen.dart`/`admin_invite_screen.dart` (birim dropdown'larına is_active filtresi).

### Faz 5 — SLA + istatistikler ✅ TAMAMLANDI (2026-07-21)
- Talep çözüm süresi: `requests.resolved_at` zaman damgası; talep detayında "X saat/gün içinde çözüldü" gösterimi.
- SLA/eskalasyon: sabit 3 gün (kullanıcı kararı — ayar tablosu YOK) boyunca çözülmemiş (`acik`/`cozuldu`) talep için müdüre (yoksa admin'e) uyarı bildirimi. **Yaklaşım: uygulama açılışında kontrol** (pg_cron KULLANILMADI — bu self-hosted kurulumda etkin değil, etkinleştirmek `shared_preload_libraries` + container yeniden yapılandırması gerektirirdi, Faz 2'deki Inbucket'a benzer bir altyapı riski; app-launch kontrolü yeni altyapı gerektirmedi).
- Admin istatistik ekranı (toplam talep, birim×durum bazlı sayılar, ortalama çözüm süresi) + müdür istatistik ekranı (aynısının kendi birimi versiyonu, tek RPC yerine iki ayrı RPC — dönüş şeklini role'e göre değiştiren tek bir RPC yerine, bu kod tabanının "rol dallanmasını Dart'a sızdırma" ilkesiyle tutarlı).
- **Etkilenen yerler:** `requests` (+`resolved_at`, +`sla_notified_at`), `sync_request_status_from_result()` trigger'ının yeniden tanımı, YENİ `check_sla_breaches()`/`get_admin_stats()`/`get_manager_stats()` RPC'leri, YENİ `admin_stats_screen.dart`/`manager_stats_screen.dart`, `request_detail_screen.dart` (çözüm süresi satırı), `home_screen.dart` (`_isMudur`, SLA tetikleme, İstatistikler butonu).
- **Bilinen sınırlama:** `resolved_at`, sadece bugünden (2026-07-21) itibaren yapılan onaylarda doluyor — geçmişte onaylanmış talepler için geriye dönük doldurma yapılmadı (o anki onay zamanı hiçbir yerde kayıtlı değildi, tahmini bir değer yazmak yanıltıcı olurdu). Bu yüzden istatistik ekranlarındaki "ortalama çözüm süresi" şu an mevcut test verisiyle boş görünüyor, yeni onaylar biriktikçe dolacak.
- **Kapsam netleştirmesi (önemli):** Bu fazın başında kullanıcı, "Faz 5" adı altında MVP çekirdeğinin (talep oluşturma/listeleme/atama/çözümleme/onay) model sınıfı + service katmanı + `StreamBuilder` ile YENİDEN inşa edilmesini istedi — ama bu akış zaten roadmap madde 5'te (2026-07-16) tamamlanmış, çalışan, test edilmiş bir özellikti (`request_create_screen.dart` vb.), sadece farklı bir isimle karıştırılmıştı. Bu, kullanıcıya açıkça belirtilip netleştirildi; gerçek Faz 5 (bu bölüm) planlandı.

## İlerleme Günlüğü
- **2026-07-08:** Proje CLAUDE.md ile başlatıldı, git ve GitHub bağlantısı kuruluyor.
- **2026-07-08:** Git deposu kuruldu, GitHub'daki (MurselAktop/talep_sorgu_cevap_app) mevcut Flutter .gitignore ile birleştirildi, ilk commit push edildi.
- **2026-07-08:** Git/GitHub push sonrası Flutter proje iskeleti kuruldu.
- **2026-07-08:** Supabase'e anon key ile bağlantı kuruldu.
- **2026-07-08:** Proje, Flutter taramasında "ü" (Türkçe karakter) kaynaklı hatalar yüzünden `OneDrive\Masaüstü\Talep_Sorgu_Cevap_App` konumundan `C:\Projeler\Talep_Sorgu_Cevap_App` konumuna taşındı.
- **2026-07-08:** Talep dağıtım modeli havuzdan atamaya değiştirildi (birim müdürü isteği üzerine).
- **2026-07-08:** `main.dart`'taki `ColorScheme.fromSeed` düzeltmesi sonrası uygulama Edge (web) hedefinde başarıyla çalıştırılıp Supabase bağlantısı doğrulandı. Windows masaüstü hedefi, makinede Visual Studio C++ build araçları eksik olduğu için şu an çalışmıyor (kodla ilgili değil, ileride giderilebilir).
- **2026-07-08:** Madde 2 (Flutter iskeleti + Supabase paketi) tamamlandı olarak işaretlendi.
- **2026-07-08:** Tablo adı tickets'tan requests'e değiştirildi, RLS planı netleştirildi.
- **2026-07-09:** Git + GitHub bağlantısı tamamlandı; commit/push disiplini olarak "günde sık commit, gün sonunda birleştirip (squash) tek seferde push" yöntemi benimsendi.
- **2026-07-09:** Flutter proje iskeleti kuruldu, klasör Türkçe karakter içeren yoldan (Masaüstü) Türkçe karaktersiz bir konuma taşındı.
- **2026-07-09:** Supabase bağlantısı kuruldu: flutter_dotenv paketi eklendi, .env + .env.example ayrımı yapıldı, .gitignore'a .env eklendi, lib/services/supabase_service.dart oluşturuldu, main.dart güncellendi.
- **2026-07-09:** Veri modeli tabloları Supabase Studio (Table Editor) üzerinden elle, tıklanarak oluşturuldu: departments, users (auth.users'a foreign key ile bağlı), requests, results, notifications — hepsi ilgili foreign key bağlantılarıyla birlikte kuruldu.
- **2026-07-09:** Talep dağıtım modeli, havuzdan atama modeline değiştirildi: müdür talebi belirli bir personele atar (assigned_to), birimdeki diğer personel görebilir ama sadece atanan çözebilir.
- **2026-07-09:** departments ve users tabloları için RLS politikaları tamamlandı. requests tablosu için 7 RLS politikası (3 SELECT, 1 INSERT, 3 UPDATE) planlandı/yazıldı.
- **2026-07-09:** results ve notifications tabloları için RLS henüz yazılmadı, sıradaki adım bu.
- **2026-07-09:** Öğrenilen teknik dersler: (1) Supabase Studio'da bir sütunun default value'sunu değiştirirken "syntax error" alınırsa, en güvenilir çözüm sütunu silip yeniden oluşturmak; (2) foreign key eklenmiş bir sütunun tipini sonradan değiştirmek problemli olabiliyor, bu yüzden sütun ayarları (tip, nullable, default value) foreign key eklenmeden önce tamamlanmalı; (3) Table Editor'de birden fazla değişikliği aynı anda kaydetmeye çalışmak hatalara yol açabiliyor, her değişiklik tek tek kaydedilmeli.
- **2026-07-09:** Sıradaki adım: results ve notifications tablolarının RLS'lerini tamamlamak, sonra tüm tablo + RLS yapısını tek bir SQL migration dosyası olarak dışa aktarıp commit'lemek.
- **2026-07-09:** Tüm RLS (Row Level Security) planı tamamlandı — 5 tablo, 22 politika, Supabase Studio üzerinden elle oluşturuldu ve doğrulandı.
  - departments: herkes okur, sadece admin ekler/günceller.
  - users: kendi profili + müdür kendi birimi + admin herkes (SELECT); kendi kaydını oluşturma (INSERT); kendi profilini güncelleme, rol/department_id değişmeden (UPDATE).
  - requests: açan kişi + birim çalışanları + admin (SELECT, 3 kural); giriş yapmış herkes talep açabilir (INSERT); müdür durum/atama günceller + açan kişi henüz atanmamışsa düzenler/iptal eder + admin koşulsuz günceller (UPDATE, 3 kural).
  - results: ilgili talebi görebilen sonucu görebilir (SELECT); sadece atanan personel rapor ekler (INSERT); atanan personel beklemedeyken düzenler + müdür onaylar/reddeder (UPDATE, 2 kural).
  - notifications: kendi bildirimini görme (SELECT); sadece sistem/trigger ekleyebilir, normal kullanıcı elle ekleyemez — with check (false) (INSERT); kendi bildirimini okundu işaretleme (UPDATE).
  - Öğrenilen ek dersler: (1) bir kuralı yanlış tabloya eklemek mümkün olduğu için, tüm tablolardaki politika sayıları ve isimleri planla karşılaştırılarak tek tek doğrulandı; (2) `exists (select 1 from ... where ...)` kalıbı, bir tablonun RLS kuralının başka bir tablodaki (örn. requests) ilişkili kayda bakması gerektiğinde kullanıldı (örn. results tablosunun, bağlı olduğu request'in görülebilirlik kurallarını miras alması).
  - Sıradaki adım: Tüm tablo yapısı + RLS politikalarını tek bir SQL migration dosyası olarak dışa aktarıp git'e commit'lemek, ardından Flutter tarafında auth ekranlarına (giriş/kayıt) geçmek.
- **2026-07-10:** Swagger UI, Docker üzerinde kuruldu ve üç teknik engel (apikey'siz şema erişimi, yanlış host adresi, eksik securityDefinitions) sırayla çözülerek tam çalışır hale getirildi.
- **2026-07-10:** users tablosunda kritik bir RLS hatası (infinite recursion, kod 42P17) tespit edildi ve current_user_role()/current_user_department() adlı security definer fonksiyonlarıyla çözüldü.
- **2026-07-10:** Tüm 5 tablo (departments, users, requests, results, notifications) hem terminal (curl) hem Swagger UI üzerinden GET istekleriyle test edildi — hepsi 200 ve boş liste [] döndürdü, RLS'in gerçekten çalıştığı doğrulandı.
- **2026-07-10:** Sıradaki adım: Migration dosyasını çıkarıp commit'lemek, ardından Flutter auth ekranlarına geçmek.
- **2026-07-10:** Kullanıcı kaydı (signUp) ile public.users profili arasındaki senkronizasyon için trigger tabanlı yöntem uygulandı: handle_new_user() adlı security definer fonksiyonu ve auth.users üzerinde after insert tetiklenen on_auth_user_created trigger'ı oluşturuldu. Fonksiyon, raw_user_meta_data içindeki full_name'i okuyup public.users'a role='vatandas', department_id=null ile otomatik satır ekliyor.
  - Kurulum pg_proc (prosecdef = true) ve pg_trigger sorgularıyla doğrulandı — fonksiyon ve trigger veritabanında gerçekten mevcut.
  - Henüz yapılmadı / sıradaki adım: gerçek bir signUp isteğiyle uçtan uca test (auth.users + public.users'a doğru yazılıyor mu doğrulamak). Bu test tamamlanınca migration dosyası çıkarılıp commit'lenecek, ardından MVP akışına geçilecek.
- **2026-07-13:** signUp testinde SMTP servisi tanımlı olmadığı için e-posta doğrulama maili gönderimi başarısız oluyor ve tüm kayıt 500 hatasıyla rollback ediliyor. Yerel geliştirmede GOTRUE_MAILER_AUTOCONFIRM=true ile e-posta doğrulaması geçici olarak devre dışı bırakıldı (SMTP servisi henüz bağlı değil). Production'a geçmeden önce gerçek bir SMTP servisi bağlanıp bu ayar kapatılmalı.
  - Not: Ayar, ana Supabase compose dosyasında (`C:\Users\makto\supabase\docker\docker-compose.yml`, bu repo dışında) zaten `${ENABLE_EMAIL_AUTOCONFIRM}` değişkenine bağlıydı; asıl değişiklik aynı klasördeki `.env` dosyasında `ENABLE_EMAIL_AUTOCONFIRM=false` → `true` yapmak oldu, ardından `docker compose up -d auth` ile auth servisi yeniden oluşturuldu.
- **2026-07-13:** Kullanıcı kaydı (signUp) uçtan uca gerçek bir istekle test edildi ve başarıyla doğrulandı: Swagger UI üzerinden atılan bir signup isteği 200 döndü, auth.users'a kayıt düştü, handle_new_user() trigger'ı çalışıp public.users'a otomatik profil (role='vatandas', department_id=null, full_name doğru) oluşturdu. Bu, projenin en kritik akışlarından birinin (kimlik doğrulama ↔ profil senkronizasyonu) uçtan uca çalıştığının kanıtı oldu.
  - Test sırasında iki ayrı sorun tespit edilip çözüldü: (1) yerel Docker kurulumunda SMTP servisi tanımlı olmadığı için kayıt sonrası e-posta doğrulama gönderimi başarısız oluyor ve tüm kaydı geri alıyordu (rollback, HTTP 500); GOTRUE_MAILER_AUTOCONFIRM=true ayarıyla yerel test için e-posta doğrulaması geçici olarak devre dışı bırakıldı, kullanıcılar artık otomatik onaylanıyor (email_confirmed_at anında dolduruluyor). (2) Swagger UI'a, REST API şemasının yanına ikinci bir şema (swagger/auth-openapi.json) eklenerek Auth API (signup, token) endpoint'leri de görsel olarak test edilebilir hale getirildi.
  - ÖNEMLİ NOT: GOTRUE_MAILER_AUTOCONFIRM=true sadece yerel geliştirme içindir. Production'a geçmeden önce gerçek bir SMTP servisi bağlanıp bu ayar kapatılmalı, aksi halde gerçek kullanıcılar e-posta doğrulaması yapmadan hesap açabilir.
  - Auth akışının uçtan uca çalıştığı doğrulandığı için, ertelenen migration dosyasının çıkarılması artık bir sonraki adım olarak netleşti.
- **2026-07-13:** Flutter tarafında giriş (login) ve kayıt (register) ekranları oluşturuldu; AuthGate ile uygulama açılışında oturum kontrolü yapılıp kullanıcı ilgili ekrana yönlendiriliyor.
  - Kayıt sonrası bilinçli olarak signOut() çağrılıyor (GOTRUE_MAILER_AUTOCONFIRM açık olduğu için signUp otomatik oturum açtırıyor, kullanıcı yine de login ekranına yönlendiriliyor).
  - Ad-soyad alanındaki bir doğrulama açığı (sadece boşluk karakteri geçerli sayılıyordu) düzeltildi.
  - Tüm akış gerçek Flutter arayüzünden uçtan uca test edildi: kayıt, login ekranına yönlendirme, yanlış şifre hata mesajı, doğru şifreyle giriş ve ana ekrana ulaşma — hepsi başarılı.
  - Migration dosyası daha önce push edildi. Sıradaki adım: talep oluşturma ve listeleme ekranları (MVP'nin çekirdek akışı).
- **2026-07-13:** Birim danışman hocasıyla istişare sonrası, vatandaş erişimi için hibrit model (hesaplı + anonim kod ile sorgulama) ve personel kaydı için davet kodu tabanlı model tasarım kararı olarak netleşti. Henüz kod olarak uygulanmadı, sıradaki adım olarak planlandı.
- **2026-07-13:** handle_new_user() trigger'ı, kayıt sırasında invite_code gönderilip gönderilmediğine göre dallanacak şekilde güncellendi: kod varsa personnel_invites tablosundan role/department_id okunup personel profili oluşturuluyor ve kod "kullanıldı" işaretleniyor; kod yoksa eskisi gibi vatandaş profili oluşturuluyor. Dört senaryo (geçerli kod, tekrar kullanım reddi, kodsuz kayıt, veritabanı doğrulamaları) Swagger UI üzerinden test edilip doğrulandı.
- **2026-07-13:** Admin davet kodu oluşturma ekranı (admin_invite_screen.dart), personel kayıt ekranı (personnel_register_screen.dart) ve giriş ekranının Vatandaş Girişi/Personel Girişi olarak ikiye ayrılması + giriş sonrası rol doğrulaması (login_screen.dart) tamamlandı. Uçtan uca test edildi: admin daveti oluşturma, o davetle personel kaydı, doğru/yanlış giriş türü senaryoları, rol uyuşmazlığında otomatik çıkış ve Türkçe hata mesajı — hepsi doğrulandı.
- **2026-07-14:** Hibrit vatandaş erişimi (hesaplı + anonim erişim kodu) ve davet kodu tabanlı personel kaydı mimarisinin veritabanı tarafı tamamlandı:
  - `personnel_invites` tablosu oluşturuldu: `code` (unique, `generate_invite_code()` ile otomatik üretilir), `department_id` (departments'a FK, `on delete cascade`), `role`, `created_by`/`used_by` (users'a FK, `on delete set null`, nullable), `used` (boolean), `created_at`, `used_at`. FK kararlarının gerekçeleri "personnel_invites Foreign Key Kararları" bölümüne eklendi.
  - `generate_invite_code()` ve `generate_access_token()` fonksiyonları eklendi (karışıklık yaratan karakterler hariç rastgele, insan tarafından okunabilir kod üretimi).
  - `personnel_invites` üzerinde admin-only RLS politikaları uygulandı (`current_user_role() = 'admin'` kontrolüyle SELECT/INSERT/UPDATE/DELETE).
  - `requests` tablosuna `access_token` sütunu eklendi (text, NOT NULL, unique, default `generate_access_token()`); `created_by` nullable yapıldı (anonim talepler için); `requester_type` için `in ('vatandas', 'anonim', 'personel')` CHECK constraint eklendi.
  - `handle_new_user()` trigger fonksiyonu, kayıt sırasında `invite_code` gönderilip gönderilmediğine göre dallanacak şekilde yeniden yazıldı: kod varsa `personnel_invites`'tan `for update` ile satır kilitlenip role/department_id okunuyor ve personel kaydı yapılıyor (kod geçersiz/kullanılmışsa `raise exception` ile tüm kayıt reddediliyor), kod yoksa eskisi gibi `role = 'vatandas'` ile kayıt yapılıyor.
  - `requests` tablosuna anonim `INSERT` için ek bir RLS politikası eklendi (`to anon with check (created_by is null)`).
  - `create_request(p_title, p_description, p_category, p_department_id, p_requester_type, p_created_by)` adında `security definer` bir RPC fonksiyonu yazıldı; `auth.uid()` ile `p_created_by`/`p_requester_type` doğrulaması yaparak kimlik sahteciliğini engelliyor, talebi ekleyip `access_token`'ı döndürüyor. Bu, PostgREST'in `.insert().select()` ikili SELECT+INSERT RLS gereksinimini aşmak için `security definer` deseni kullanılarak çözüldü.
  - `get_request_by_token(p_access_token)` adında `security definer` bir RPC fonksiyonu yazıldı; `requests`+`departments` join edip title/description/category/status/created_at/department_name döndürüyor. Güvenlik sertleştirmesi olarak `requester_type in ('vatandas','anonim')` filtresi eklendi — personel taleplerinin token ile sorgulanamamasını garanti ediyor.
- **2026-07-14:** Aynı mimarinin Flutter tarafı tamamlandı:
  - `lib/screens/login_screen.dart` yeniden yazıldı: Vatandaş Girişi / Personel Girişi / Giriş Yapmadan Devam Et seçim ekranı, paylaşılan form widget'ı, giriş sonrası `public.users.role` kontrolü (uyuşmazsa otomatik `signOut` + Türkçe hata).
  - `lib/screens/register_screen.dart` ve `lib/screens/personnel_register_screen.dart`'a inline hata mesajları eklendi (zaten kayıtlı e-posta, geçersiz/kullanılmış davet kodu — artık genel SnackBar yerine ilgili `TextFormField`'ın altında gösteriliyor).
  - `lib/screens/admin_invite_screen.dart` (yeni) — admin'in birim+rol seçip davet kodu ürettiği, kopyalanabilir kod gösteren ve geçmiş davetleri listeleyen ekran.
  - `lib/screens/citizen_guest_menu_screen.dart` (yeni) — "Talep Oluştur" ve "Sonucu Sorgula" menüsü.
  - `lib/screens/request_create_screen.dart` (yeni) — talep formu; `requester_type`/`created_by` giriş durumuna göre otomatik belirleniyor (giriş yoksa anonim, vatandaşsa vatandas, personelse personel); `create_request` RPC'sini çağırıp dönen `access_token`'ı kullanıcıya kopyalanabilir şekilde gösteriyor.
  - `lib/screens/query_result_screen.dart` (yeni) — talep kodu girip `get_request_by_token` RPC'sini çağıran, sonucu Türkçe etiketlerle (durum, kategori vb.) gösteren ekran.
  - `lib/screens/home_screen.dart`, `StatefulWidget`'a çevrilip giriş yapmış herhangi bir kullanıcı (vatandaş VEYA personel) için "Talep Oluştur" butonu gösterilecek şekilde düzeltildi (personelin de kurum-içi arıza/talep açabilmesi gerektiği için).
- **2026-07-14:** Hibrit vatandaş erişim mimarisi tamamlandı: personnel_invites tablosu + davet kodu üretimi (generate_invite_code), requests.access_token (generate_access_token) ile anonim/vatandaş sorgu takibi, requests.requester_type (vatandas/anonim/personel) ayrımı, handle_new_user() trigger'ının davet koduna göre dallanması.
- **2026-07-14:** Anonim talep oluşturma sorunu (PostgREST'in insert+select ikili RLS gereksinimi) create_request() security definer RPC fonksiyonuyla çözüldü; kimlik sahteciliğine karşı auth.uid() doğrulamasıyla sertleştirildi.
- **2026-07-14:** get_request_by_token() RPC'si, requester_type filtresiyle sertleştirildi — personel talepleri artık anonim token sorgusuyla asla görüntülenemiyor.
- **2026-07-14:** Flutter tarafında tamamlanan ekranlar: login_screen (vatandaş/personel/misafir girişi ayrımı), register_screen ve personnel_register_screen (inline hata mesajları), admin_invite_screen (davet kodu üretimi ve geçmişi), citizen_guest_menu_screen, request_create_screen, query_result_screen, request_list_screen (Gelen Talepler), my_requests_screen (Taleplerim), home_screen (rol bazlı buton görünürlüğü, Çıkış Yap butonu).
- **2026-07-14:** Güvenlik sertleştirmesi: login_screen'deki rol uyuşmazlığı hata mesajı, hesabın var olup olmadığını/rolünü sızdırmaması için genel bir mesaja indirgendi (register_screen'deki e-posta sızıntısı dersiyle aynı prensip).
- **2026-07-14:** Önemli mimari ders: RLS bir kullanıcının görebileceği ÜST SINIRI belirler, hangi EKRANIN o sorguyu attığını bilmez. Aynı sorguyu (ek filtresiz) atan iki farklı ekran, aynı kullanıcı için her zaman aynı (RLS'in izin verdiği tam) sonucu döndürür. Bir ekranın RLS'in izin verdiği kümenin sadece bir ALT KÜMESİNİ göstermesi gerekiyorsa (örn. "Taleplerim" sadece kendi oluşturduklarını, "Gelen Talepler" sadece atananı/birimi göstermeli), istemci tarafında ek WHERE filtresi (örn. .eq('created_by', ...) veya .eq('assigned_to', ...)) yazılması ŞART — RLS'e güvenmek tek başına yeterli değil.
- **2026-07-14:** CLAUDE.md'deki "Şeffaflık" kararından (personel biriminin tüm taleplerini görür) bilinçli olarak sapıldı: personel artık SADECE kendisine atanan talepleri görüyor (assigned_to = auth.uid()), müdür ise atama yapabilmek için biriminin tüm taleplerini görmeye devam ediyor. RLS, "birim_personeli_kendi_talepleri_gorebilir" kuralı kaldırılıp mudur_biriminin_tum_taleplerini_gorebilir / personel_atanan_talepleri_gorebilir / admin_tum_talepleri_gorebilir olarak üçe bölünerek güncellendi.
- **2026-07-14:** Bilinen eksikler / sıradaki adımlar: talep detay ekranı, talep atama ekranı (müdür → personel, assigned_to güncellemesi), talep çözümleme ekranı (results tablosu), onay ekranı (müdür onay/red); migration dosyasının güncellenmesi; GOTRUE_MAILER_AUTOCONFIRM'in production öncesi kapatılması; git commit+push'un yapılması; get_request_by_token'daki not_found/forbidden mesaj ayrımının bilinçli olarak ertelenmesi.
- **2026-07-14:** requests tablosu için görünürlük modeli değiştirildi: birim_personeli_kendi_talepleri_gorebilir kuralı kaldırılıp, mudur_biriminin_tum_taleplerini_gorebilir (müdür → biriminin tüm talepleri) ve personel_atanan_talepleri_gorebilir (personel → SADECE assigned_to = kendisi) olarak ikiye ayrıldı; admin_tum_talepleri_gorebilir ayrı bir kural olarak eklendi. Bu, CLAUDE.md'deki eski "şeffaflık" kararından bilinçli bir sapmadır.
- **2026-07-14:** Test sürecinde önemli bir teşhis vakası yaşandı: müdür test hesabı (mudur1.test@test.com) davet kodu sorunuyla sessizce oluşturulamamıştı (trigger'ın raise exception ile tüm kaydı geri alması); test sırasında bu fark edilmeden hâlâ admin oturumuyla test yapılmış, bu da "müdürün her iki birimi de gördüğü" yanlış izlenimini verdi. SQL ile auth.users + public.users join sorgusuyla hesabın hiç var olmadığı doğrulandı, hesap dikkatlice yeniden oluşturularak çözüldü. Genel ders: bir test beklenmedik sonuç verdiğinde, önce "gerçekten doğru hesapla mı test ediyorum" diye kontrol etmek gerekir.
- **2026-07-14:** MVP'nin kalan adımları netleştirildi ve önceliklendirildi: (A) birbirine bağımlı, teker teker yapılacak ekranlar — talep detay, düzenleme/iptal, atama, çözümleme, onay, bildirim; (B) bağımsız, A bittikten sonra yapılacak işler — migration dosyası güncelleme, GOTRUE_MAILER_AUTOCONFIRM kapatma (production öncesi), git commit+push. A grubunun tek seferde değil, önceki adımlarda olduğu gibi teker teker ve her birinin ayrı test edilmesi kararlaştırıldı (RLS-istemci filtresi ayrımı ve müdür kaydı vakalarında küçük adımların hataları yakalamadaki değeri kanıtlandığı için).
- **2026-07-16:** MVP'nin çekirdek fonksiyonel döngüsü tamamlandı: talep oluşturma → listeleme/sorgulama → detay görüntüleme → atama (müdür→personel) → çözümleme (personel raporu) → onay/red (müdür) → red sonrası yeniden düzenleyip gönderme → bildirim (onay/red anında ilgili kişiye uygulama içi bildirim) tüm akış uçtan uca test edildi ve doğrulandı. Ayrıca: results.previously_rejected sütunu + trigger'ı ile "bu rapor daha önce reddedilmişti" bilgisi kalıcı olarak işaretleniyor; requests.status için CHECK kısıtlaması olmadığı için 'iptal' durumu eklendi. Sıradaki adım: migration dosyasının güncellenmesi, GOTRUE_MAILER_AUTOCONFIRM ayarının gözden geçirilmesi, git commit+push.
- **2026-07-16:** A grubu (MVP çekirdek fonksiyonel döngüsü — talep detay, düzenleme/iptal, atama, çözümleme, onay, bildirim ekranları) resmi olarak tamamlandı kabul edildi; roadmap madde 5 ve MVP tamamlanma kontrol listesindeki 8 madde de zaten ✅. `supabase db diff --schema public` ile şema kontrolü yapıldı: "No schema changes found" — A grubu geliştirmesi sırasında (previously_rejected sütunu, sync trigger'ları dahil) şemada zaten önceki migration'a yansıtılmamış hiçbir fark kalmadığı doğrulandı, yeni migration dosyası oluşturulmadı. GOTRUE_MAILER_AUTOCONFIRM notu ayrı bir "Yapılacaklar / Production Öncesi Kontrol Listesi" bölümüne taşındı. Sıradaki adım: B grubu — Medya ekleri (foto/video, roadmap madde 6) veya push bildirimler (madde 7).
- **2026-07-16:** Android mobil hedefi için sağlık kontrolü yapıldı: `flutter doctor -v` Android toolchain'i geçerli gösterdi (Android SDK 36.1.0, tüm lisanslar kabul edilmiş). Android Studio Device Manager üzerinden **Pixel 8** emülatörü oluşturuldu (`flutter emulators` listesinde görünüyor). `flutter run -d Pixel_8` ile uygulama emülatörde başlatıldı ve ekranda göründü; ancak konsolda hata/uyarı olup olmadığı net doğrulanmadı. Bu sadece bir **açılış/sağlık kontrolüdür** — giriş, talep oluşturma gibi hiçbir akış bu adımda elle test edilmedi (2026-07-08 tarihli kayıtta belirtilen "Windows masaüstü hedefi çalışmıyor" sorununa karşı Android'in canlı bir mobil test hedefi olarak kullanılabilir olduğunu doğrular). Sıradaki adım: uygulamanın Android emülatöründe temel akışlarla (giriş, talep oluşturma vb.) fiilen manuel test edilmesi.
- **2026-07-17:** Android emülatör sağlık kontrolü tamamlandı, MVP'nin tüm çekirdek akışları gerçek platformda doğrulandı: vatandaş talep oluşturma, müdür atama, personel çözümleme, müdür onay/red, bildirim — hepsi emülatörde uçtan uca başarıyla test edildi.
- **2026-07-17:** Roadmap madde 6 (Medya ekleri) backend tarafı tamamlandı: `attachments` tablosu, RLS politikaları ve `request-attachments` Storage bucket'ı eklendi (canlı DB'de doğrulandı, commit edildi). Ardından kapsam genişletildi: `attachments.media_type` CHECK constraint'ine ve bucket'ın `allowed_mime_types` listesine `document` (PDF, DOCX) desteği eklendi — hem migration dosyası hem canlı DB güncellendi ve doğrulandı.
- **2026-07-17:** Flutter tarafında `request_create_screen.dart`'a medya seçme/önizleme eklendi (galeri/kamera foto-video `image_picker`, belge `file_picker`). `file_picker`'ın en güncel sürümü (11.0.2) projenin AGP 9 kurulumuyla derleme hatası verdiği (Kotlin dosyaları derlenmiyor) için `file_picker: 10.3.10`'a sabitlendi — bu sürüm hem Kotlin eklentisini koşulsuz uyguluyor hem `compileSdk`'i projeyle dinamik eşleştiriyor, ikisi de çalışıyor.
- **2026-07-17:** Medya eklerinin gerçek Storage yükleme + `attachments` kaydı mantığı tamamlandı: "Gönder"e basılınca önce talep oluşturuluyor, sonra seçilen her dosya `request-attachments` bucket'ına `{request_id}/{benzersiz_ad}` yoluna yükleniyor ve `attachments` tablosuna kaydediliyor. Bunun için `create_request` RPC'sinin dönüş tipi `text` (sadece access_token) yerine `jsonb` (`{id, access_token}`) olarak değiştirildi — Flutter tarafının Storage yolu için talebin `id`'sine ihtiyacı olduğundan (canlı DB'de `DROP`+`CREATE`+`GRANT` ile uygulandı, doğrulandı). Bir dosya yüklenemezse talep geri alınmıyor, sadece hangi dosyanın başarısız olduğu kullanıcıya bildiriliyor.
- **2026-07-17:** Eklerin GÖRÜNTÜLENMESİ eklendi (`request_detail_screen.dart`) — talep yüklenince `attachments` tablosu da sorgulanıp her dosya için `createSignedUrl` (private bucket olduğu için) alınıyor; fotoğraflar küçük resim olarak gösteriliyor (tıklanınca tam ekran önizleme açılıyor), video/belge ikon + dosya adıyla temsil ediliyor. Bu yükleme, talebin geri kalanının görüntülenmesini engellememesi için sessizce hataya toleranslı (`_loadCurrentUserRole` ile aynı desen). Böylece roadmap madde 6 (Medya ekleri) uçtan uca (seç → yükle → görüntüle) tamamlandı.
- **2026-07-17:** İki küçük düzeltme: (1) `request_create_screen.dart`'ta "Gönder"e peş peşe tıklayınca aynı talebin 4-5 kez oluşturulduğu tespit edildi — sebep, `_isSubmitting` kontrolünün sadece butonun `onPressed`inde olup `_submit()`in kendisinde olmamasıydı (rebuild ile buton devre dışı kalana kadar araya giren tıklamalar yeni bir çağrı başlatabiliyordu). `_submit()`in en başına senkron bir `if (_isSubmitting) return;` koruması eklendi. Ayrıca başarı akışı ayrıştırıldı: anonim talepler için erişim kodu dialogu aynen korunuyor (tek bulma yolu olduğu için), giriş yapmış kullanıcılar (vatandaş/personel) için ise kısa bir SnackBar gösterilip doğrudan "Taleplerim" ekranına yönlendiriliyor. (2) Müdürün talep açıklamasını göremediği bildirilen sorun araştırıldı — `request_detail_screen.dart`'ta `description` hem sorguda (`select('*, ...')`) hem UI'da (`_buildInfoRow('Açıklama:', ...)`) zaten mevcuttu, kod tarafında bir hata bulunamadı; muhtemelen o günkü ardışık `flutter clean` yeniden derlemelerinden birinde eski build ile test edilmiş, yeniden test edilince düzeldiği doğrulandı.
- **2026-07-20:** Beş fazlık genişletme planı (Faz 1: veri modeli + kayıt genişletmesi + KVKK; Faz 2: hesap yönetimi; Faz 3: liste arama/filtreleme; Faz 4: admin/müdür yönetim araçları; Faz 5: SLA + istatistikler) roadmap'e madde 7-11 olarak işlendi; push bildirimleri (eski madde 7) bilinçli olarak bu fazların sonrasına (madde 12) ertelendi. Faz 1 kararları kullanıcıyla netleştirildi: tc_no sadece kendisi + admin görebilir (bu yüzden ayrı `users_private` tablosu — RLS satır bazlı olduğu, sütun gizleyemediği için), tüm yeni alanlar (tc_no, telefon, il, ilçe) kayıtta zorunlu, her iki kayıt ekranı da kapsamda, tc_no DB'de CHECK + UNIQUE ile korunacak. Henüz hiçbir kod/DB değişikliği yapılmadı — Faz 1 detay planı kullanıcı onayına sunuldu.
- **2026-07-20:** Genişletme Faz 1 uygulandı (kullanıcı onayıyla; KVKK bloğu onay sırasında kapsamdan çıkarılıp production öncesi kontrol listesine taşındı):
  - Veritabanı: `users` tablosuna `phone`, `il`, `ilce` (nullable) eklendi; `users_private` tablosu oluşturuldu (`tc_no` CHECK `^[1-9][0-9]{10}$` + UNIQUE, users'a `on delete cascade` FK). RLS: sadece 2 SELECT politikası (kendisi + admin) — INSERT/UPDATE/DELETE politikası BİLİNÇLİ olarak yok, satırı yalnızca `handle_new_user()` (security definer) ekler, tc_no uygulama üzerinden değiştirilemez. Migration dosyasına eklendi, canlı DB'ye `docker exec + psql --single-transaction` ile uygulandı.
  - `handle_new_user()`: tc_no/phone/il/ilce metadata alanları YENİ kayıtlar için zorunlu (eksikse `raise exception` — form atlanarak doğrudan API'ye istek atılsa bile kayıt reddediliyor); phone/il/ilce `users`'a, tc_no `users_private`'a yazılıyor (aynı trigger içinde → atomik); çift tc_no (`unique_violation`) ve format hatası (`check_violation`) Türkçe mesajlara çevriliyor.
  - Flutter: `lib/utils/validators.dart` (T.C. no resmi algoritma kontrolü, telefon doğrulama+normalizasyon, genel zorunlu alan) ve `lib/constants/turkiye_iller.dart` (81 il, dropdown için — serbest metin il kirliliğini önlemek amacıyla) oluşturuldu. `AuthService.signUp`'a 4 zorunlu parametre eklendi. `register_screen.dart` ve `personnel_register_screen.dart`'a yeni alanlar (T.C. no, telefon, il dropdown, ilçe serbest metin) + çift tc_no için inline hata gösterimi eklendi; formlar uzadığı için `SingleChildScrollView`'a alındı.
  - Testler (curl + SQL ile, hepsi geçti): eksik metadata ile signup reddi + rollback doğrulaması (auth.users'a satır düşmüyor), geçerli kayıtta iki tabloya doğru yazım, aynı tc_no ile ikinci kayıt reddi (Türkçe mesaj + rollback), geçersiz format reddi, RLS senaryoları (kendisi 1 satır / müdür 0 satır / admin hepsi / UPDATE 0 satır etkiledi), davet kodlu personel kaydının yeni alanlarla regresyonu (rol/birim doğru, kod kullanıldı işaretlendi). Test hesapları ve test davet kodu temizlendi (cascade silme de bu sırada doğrulandı).
  - Emülatörde manuel test ve git commit henüz yapılmadı — emülatör testi kullanıcı tarafından yapılacak.
- **2026-07-20 (kök neden düzeltmesi):** Faz 1'in çift tc_no/davet kodu hata mesajlarının UI'da neden hep generic çıktığı araştırıldı ve kök neden bulundu: `gotrue` paketinin her `/signup` isteğine otomatik eklediği `X-Supabase-Api-Version: 2024-01-01` header'ı, sunucudaki GoTrue'yu `handle_new_user()` trigger'ından gelen HER özel Postgres hatasını generic `"Database error saving new user"` mesajına sarmalamaya zorluyor (bkz. "Bilinen Ortam Notları" — ayrıntılı teknik ders). Bu, sadece tc_no'yu değil, `personnel_register_screen.dart`'taki davet kodu hata mesajını da etkiliyordu; 2026-07-13 tarihli "test edildi" notu, bu header'ı göndermeyen Swagger UI ile yapıldığı için yanıltıcıydı.
  - Çözüm: `check_registration_availability(p_tc_no, p_invite_code default null)` adında yeni bir `security definer` RPC yazıldı (tc_no format+benzersizlik ve — verilirse — davet kodu geçerliliğini kontrol ediyor, sorun varsa spesifik Türkçe `raise exception`). Bu RPC, `AuthService.signUp()`'tan ÖNCE, hem `register_screen.dart` hem `personnel_register_screen.dart`'tan çağrılıyor; RPC çağrıları Postgrest üzerinden gittiği için GoTrue'nun sanitizasyonuna hiç girmiyor, `PostgrestException.message` gerçek metni güvenilir taşıyor.
  - `handle_new_user()` trigger'ındaki mevcut kontroller DEĞİŞTİRİLMEDİ — nihai güvenlik ağı olarak aynen kalıyor (yarış durumu ihtimaline karşı).
  - Test: RPC, curl ile gerçek `X-Supabase-Api-Version` header'ı gönderilerek 6 senaryoda doğrulandı (geçersiz format, çift tc_no, geçersiz davet kodu, iki ayrı başarı senaryosu) — hepsinde spesifik Türkçe mesaj/204 doğru döndü; ardından uçtan uca (RPC geç + signUp başarılı + iki tabloya doğru yazım + davet kodu "kullanıldı" işaretlendi) doğrulandı. Test hesapları temizlendi. Telefon/emülatör üzerinden manuel test kullanıcı tarafından yapılacak.
  - Ayrıca aynı oturumda: gerçek Android cihazda (USB) `10.0.2.2`'nin geçersiz olması sorunu, `device_info_plus` ile emülatör/gerçek cihaz ayrımı yapılarak çözüldü (`supabase_service.dart`); `register_screen.dart`'a ağ isteklerine `.timeout(15s)` + `TimeoutException` yakalama eklendi (buton artık gerçek bir ağ askıda kalmasında da normale dönüyor); diğer 10 ekranın ağ çağrısı denetimi yapılıp önceliklendirilmiş bir liste kullanıcıya sunuldu (henüz uygulanmadı, kullanıcı kararı bekleniyor).
- **2026-07-20 (Faz 1 kapanışı):** Genişletme Faz 1, gerçek bir Android cihazda (Samsung A56, USB bağlı, `adb reverse tcp:8000 tcp:8000` ile) uçtan uca doğrulandı — hem ağ düzeltmesi (gerçek cihaz/emülatör ayrımı) hem RPC tabanlı kayıt doğrulama (çift tc_no, geçersiz davet kodu gibi hataların artık spesifik Türkçe mesajla gösterilmesi) canlı cihazda test edildi ve çalıştığı onaylandı. Roadmap madde 7 ✅ olarak işaretlendi. Sıradaki adım: Faz 2 (Hesap yönetimi) planlaması, kullanıcı onayıyla başlayacak.
- **2026-07-21:** Faz 2 (Hesap yönetimi) planlandı ve kodlandı. Plan modunda önce keşif yapıldı (home_screen navigasyonu, auth_service.dart, validators.dart, register ekranlarının telefon alanı, RLS'in role/department_id'yi self-update'te zaten koruduğu doğrulandı), ardından üç açık karar kullanıcıya soruldu ve netleşti: telefon DB formatı E.164'e (`+905XXXXXXXXX`) geçirilecek, yerel e-posta testi için Inbucket kurulacak, profil ekranında e-posta salt-okunur kalacak, şifremi unuttum akışı sadece e-posta gönderme kısmını kapsayacak (deep link kapsam dışı). Kullanıcı planı onayladıktan sonra "hiçbir işlem için onay isteme, direkt yap" talimatıyla uygulamaya geçildi:
  - Inbucket kurulumu sırasında önemli bir risk fark edilip düzeltildi: `dev/docker-compose.dev.yml` override'ı sadece `mail` servisini değil, `db` servisine de "Always use a fresh database when developing" yorumuyla riskli bir anonim volume + `restart: 'no'` ekliyormuş. İlk `up -d` tüm servisleri (db dahil) bu override'la yeniden oluşturdu; veri kontrol edilip sağlam olduğu görüldü (bind-mount'un anonim volume'a karşı kazandığı anlaşıldı), ardından `db`/`auth`/`storage`/`studio`/`meta` sadece base config'e geri döndürülüp (`docker compose -f docker-compose.yml up -d`) `mail` container'ı olduğu gibi bırakıldı — riski ortadan kaldıran daha güvenli bir ara yol. Ders: bir dev override dosyasını körü körüne tam haliyle uygulamadan önce içeriği satır satır okumak gerekiyor, "dev kolaylığı" bazen "veriyi sıfırla" anlamına gelebiliyor.
  - `lib/utils/phone_input_formatter.dart` (yeni) yazıldı: kullanıcı yazarken `+90 (5XX) XXX XX XX` canlı biçimlendirmesi yapan bir `TextInputFormatter`; mantığı bağımsız bir `dart run` scriptiyle (adım adım yazma, yapıştırma, backspace senaryoları) doğrulandı.
  - `validators.dart`: `phone`/`normalizePhone` E.164 üretecek şekilde yeniden yazıldı, `phoneToDisplay` (DB → maskeli görünüm) eklendi.
  - `register_screen.dart`/`personnel_register_screen.dart` yeni formatlayıcıya geçirildi; gerçek bir signup curl isteğiyle `users.phone`'un `+905XXXXXXXXX` formatında yazıldığı doğrulandı (test hesabı sonra silindi).
  - `auth_service.dart`'a `updatePassword` ve `resetPasswordForEmail` eklendi.
  - `profile_screen.dart` (yeni): full_name/phone/il/ilce düzenlenebilir, email/role/birim salt-okunur; `home_screen.dart`'ın AppBar'ına Profil ikonu eklendi.
  - `change_password_screen.dart` (yeni): mevcut şifre `signInWithPassword` ile yeniden doğrulanıp `updatePassword` çağrılıyor.
  - `forgot_password_screen.dart` (yeni) + `login_screen.dart`'a "Şifremi unuttum" bağlantısı: `resetPasswordForEmail` çağrısı gerçek bir test hesabıyla denendi, ilk denemede GoTrue "unencrypted connection" hatasıyla reddetti (fake SMTP_USER/PASS, Inbucket'a plaintext bağlantıda SMTP AUTH denemesine zorluyormuş) — dev override'ın `auth` servisi için `GOTRUE_SMTP_USER`/`PASS`'i boşalttığı fark edilip SADECE `auth` container'ı `--no-deps auth` ile yeniden oluşturuldu (db'ye dokunulmadan), ardından istek 200 döndü ve e-posta Inbucket'ta (`http://localhost:9000`) göründüğü doğrulandı.
  - `flutter analyze` tüm değiştirilen/yeni dosyalarda temiz (sadece önceden var olan, ilgisiz info/warning'ler kaldı).
  - **Test durumu:** Backend/mantık seviyesinde (curl, psql, bağımsız dart script, flutter analyze) doğrulandı. Gerçek Flutter UI'da (emülatör/cihaz üzerinden profil düzenleme, şifre değiştirme, şifremi unuttum ekranlarının fiilen açılıp kullanılması) HENÜZ test edilmedi — sıradaki adım bu. CLAUDE.md'ye commit/push talimatı ayrıca kullanıcıdan istenecek.
- **2026-07-21 (şifremi unuttum: link sorunu bulundu ve düzeltildi):** Kullanıcı, "linke tıklayınca gerçekte ne oluyor" diye sordu — cevap spekülasyonla değil, gerçek bir e-postayı Inbucket'tan çekip linki bizzat `curl -D -` ile tıklatarak bulundu: link, GoTrue'nun `/verify` endpoint'ini tetikleyip `SITE_URL`'e (`http://localhost:3000`, bu projede boş) token'ı URL fragment'inde taşıyan bir 303 yönlendirmesi yapıyor — uygulama hiç açılmıyor, token hiçbir yerde okunmuyor; yani link o an fiilen İŞLEVSİZDİ. Aynı e-postanın içinde 6 haneli alternatif bir kod da olduğu fark edildi, bunun aynı tek kullanımlık token'ın ikinci temsili olduğu (biri tüketilince diğeri de geçersiz oluyor) curl ile doğrulandı. Kullanıcı, deep link (app_links paketi + Android manifest) yerine bu kod tabanlı yolu KALICI çözüm olarak onayladı:
  - YENİ `reset_password_screen.dart`: kod + yeni şifre alan bir ekran; `auth.verifyOTP(type: OtpType.recovery, email:, token:)` ile oturum alıp `AuthService.updatePassword()` çağırıyor; kod hatalı/süresi dolmuşsa (`AuthException`, GoTrue tarafında `otp_expired`) Türkçe hata gösteriyor; başarı sonrası — verifyOTP zaten geçerli bir oturum bıraktığı için — kullanıcı tekrar giriş yapmaya zorlanmadan doğrudan `HomePage`'e yönlendiriliyor.
  - `forgot_password_screen.dart` güncellendi: metinler linkten koda yönlendirecek şekilde değiştirildi ("LİNKE DEĞİL, içindeki 6 haneli koda bakın"), gönderim sonrası `ResetPasswordScreen`'e geçen bir "Kodu Girdim, Devam Et" butonu eklendi.
  - Uçtan uca curl/Inbucket testi: yanlış kod → `403 otp_expired` (Flutter'daki hata mesajıyla eşleşiyor); taze kod → `verifyOTP` başarılı → `PUT /auth/v1/user` ile şifre değiştirildi → yeni şifreyle giriş `200`, eski şifreyle giriş `400` (artık geçersiz) — hepsi doğrulandı, test hesabı temizlendi. `flutter analyze` temiz.
  - `SITE_URL=http://localhost:3000` sorununa bilinçli olarak dokunulmadı — kod tabanlı akış onu tamamen devre dışı bıraktığı için artık ilgisiz.
  - Kalan: Bu ekranın da Flutter UI'da (emülatör/cihaz) fiilen test edilmesi gerekiyor — kullanıcı bunu telefonda deneyecek.
- **2026-07-21 (Faz 2 kapanışı):** Faz 2 tamamlandı ve gerçek cihazda (Samsung A56) uçtan uca test edildi: profil ekranı, şifre değiştirme, şifremi unuttum (Inbucket + 6 haneli kod akışı, deep-link yerine), telefon numarası E.164 + otomatik biçimlendirme. Roadmap madde 8 ve "Genişletme Fazları" bölümündeki Faz 2 başlığı ✅ olarak işaretlendi. Sıradaki adım: Faz 3 (Liste kullanılabilirliği) planlaması, kullanıcı onayıyla başlayacak.
- **2026-07-21 (Faz 3):** Faz 3 (Liste kullanılabilirliği) planlandı ve kodlandı. Plan modunda önce keşif yapıldı (request_list_screen.dart ve my_requests_screen.dart'ın mevcut sorgu zincirleri, requests tablosunda status/category için CHECK kısıtlaması olmadığı, hiçbir paylaşılan widget'ın olmadığı), ardından bir Plan agent'ıyla tasarım doğrulandı (postgrest paketinin kaynağı okunarak `.or()`'un kendi içinde escape YAPMADIĞI ve `appendSearchParams`'ın farklı anahtarları otomatik AND'lediği teyit edildi). Kararlar: server-side filtreleme (client-side `.where()` yok), kategori için dropdown yerine serbest metin `ilike` (DISTINCT sorgusu PostgREST'te yok), hem filtre mantığının hem bottom sheet UI'ının `lib/widgets/request_filters.dart`'ta paylaşılması (projedeki ilk paylaşılan widget — `statusLabels` de oraya taşındı, `_requesterTypeLabels`/`_buildRequestCard` bilinçli olarak dokunulmadı).
  - Uygulama: `RequestFilters` (searchText/status/category/dateFrom/dateTo, `applyTo<T>()`, `isActive`) + `RequestFilterSheet` (durum dropdown, kategori serbest metin, `showDateRangePicker` ile tarih aralığı) yazıldı. Her iki ekrana debounce'lu (400ms, `Timer`) arama kutusu + rozet gösteren (`Badge`, `home_screen.dart`'taki bildirim rozetiyle aynı desen) bir filtre ikonu eklendi; `_loadRequests()`'teki mevcut rol/created_by filtrelerinden SONRA `order()`'dan ÖNCE `_filters.applyTo(query)` çağrısı eklendi.
  - `flutter analyze` temiz.
  - Uçtan uca doğrulama: geçici bir admin test hesabı + 4 test talebi (bilinçli olarak virgül/parantez/Türkçe karakter içeren bir başlık dahil) canlı DB'ye eklenip gerçek REST istekleriyle test edildi — arama (başlık VEYA açıklama), arama+durum AND semantiği, kategori `ilike`, ve tarih aralığı (`gte`+`lt`, dışlayıcı üst sınır) hepsi doğru sonuç döndürdü. Test sırasında bu makinedeki Windows/mingw `curl`'ün `--data-urlencode` ile Türkçe karakterleri hatalı encode edip sahte `500` hataları verdiği keşfedildi (gerçek bir backend/Dart hatası değil, sadece test aracı kusuru — bkz. "Bilinen Ortam Notları"); gerçek `RequestFilters.applyTo()` kodunun ürettiği string bir Dart scriptiyle üretilip doğru UTF-8 percent-encoding ile gönderilince sorun ortadan kalktı ve escape mantığının doğru çalıştığı teyit edildi. Test hesabı ve test talepleri temizlendi.
  - Kalan: Flutter UI'da (emülatör/cihaz üzerinden arama kutusu, filtre bottom sheet'i, rozet) fiilen test edilmedi — kullanıcı telefonda deneyecek.
- **2026-07-21 (Faz 3 kapanışı):** Faz 3 gerçek cihazda uçtan uca test edildi: arama, filtre bottom sheet, rozet, arama+filtre birlikte çalışması doğrulandı. Roadmap madde 9 ve "Genişletme Fazları" bölümündeki Faz 3 başlığı ✅ olarak işaretlendi.
- **2026-07-21 (Faz 4):** Faz 4 (Admin ve müdür yönetim araçları) planlandı ve kodlandı. Plan modunda önce keşif yapıldı (departments/users RLS'lerinin tam SQL'i, request_detail_screen.dart'ın atama deseni, login/AuthGate akışı, service_role/Edge Function altyapısının hiç olmadığı), ardından iki açık karar kullanıcıya soruldu ve netleşti: yönlendirme geçmişi ayrı bir tabloda tutulacak, yönlendirme sadece `status='acik'` taleplerle sınırlı olacak. Bir Plan agent'ıyla güvenlik tasarımı doğrulanırken KRİTİK bir bulgu ortaya çıktı: `SECURITY DEFINER` fonksiyonlar (tablo sahibi `postgres`, `FORCE ROW LEVEL SECURITY` yok) RLS'i tamamen bypass ediyor — yani RESTRICTIVE RLS politikaları tek başına yeterli değil, `create_request()` gibi her RPC'nin içine AYRICA `current_user_is_active()` kontrolü eklenmesi gerekiyordu; bu olmasaydı pasifleştirilmiş bir kullanıcı RPC üzerinden talep açmaya devam edebilirdi. Kullanıcı onayıyla "hiçbir işlem için onay isteme, direkt yap" talimatı doğrultusunda uygulamaya geçildi, her adım kendi içinde curl/psql ile (geçici test hesaplarıyla, temizlenerek) doğrulandı:
  - **Adım 1** — `departments.is_active` migration + `request_create_screen.dart`/`admin_invite_screen.dart` dropdown'larına filtre: pasifleştirilen birimin dropdown'da kaybolduğu, filtresiz (admin) sorguda görünmeye devam ettiği doğrulandı.
  - **Adım 2** — YENİ `admin_department_screen.dart` (birim ekle/düzenle/pasifleştir, `admin_invite_screen.dart`'ın liste+form desenini izliyor) + `home_screen.dart`'a giriş butonu: ekle/UNIQUE-ihlali-reddi/adını-değiştir/pasifleştir uçtan uca curl ile test edildi. (Test sırasında Faz 3'te bulunan curl/Windows UTF-8 kusuru tekrar karşımıza çıktı — Türkçe karaktersiz isimle tekrar denenince sorunsuz çalıştığı doğrulandı, gerçek bir hata değildi.)
  - **Adım 3** (en büyük/kritik adım) — `users.is_active` + YENİ `current_user_is_active()` helper + mevcut ~25 permissive politikaya dokunmadan her tabloya EK bir `AS RESTRICTIVE` politika (users/requests/results/notifications/personnel_invites/attachments) + `create_request()`'e is_active kontrolü + YENİ `admin_set_user_active()` RPC (genel bir "admin herkesi güncelleyebilir" politika YERİNE — CLAUDE.md'nin bilinçli ertelediği "admin rol yükseltme arayüzü"nü yan kapıdan açmamak için sadece is_active'a dokunan dar bir RPC) + `login_screen.dart`/`home_screen.dart`'a is_active kontrolü. `users`/`requests` gibi tablolarda "kendi satırım" carve-out'u (`id`/`created_by` = auth.uid() OR current_user_is_active()) bilinçli olarak eklendi — pasif kullanıcı kendi geçmiş verisini görmeye devam eder ama hiçbir yazma işlemi yapamaz. Regresyon testi (aktif kullanıcı için hiçbir şey bozulmadı), pasifleştirme sonrası create_request/profil güncelleme/vs'nin reddedildiği ama kendi users/requests satırının hâlâ okunabildiği (carve-out), reaktivasyon sonrası her şeyin normale döndüğü, ve `admin_set_user_active()`'ın kendi kendini pasifleştirmeyi reddettiği — hepsi gerçek test hesaplarıyla doğrulandı.
  - **Adım 4** — YENİ `admin_users_screen.dart` (kullanıcı listesi + aktif/pasif toggle, kendi hesabında gizli) + giriş butonu: departments join'li sorgu curl ile doğrulandı.
  - **Adım 5** — YENİ `request_department_reassignments` tablosu (sadece admin SELECT, sadece RPC yazar) + YENİ `reassign_request_department()` RPC (`FOR UPDATE` satır kilidi + kilit-sonrası status kontrolü, admin-veya-güncel-birimin-müdürü yetki kontrolü, hedef birim aktif mi kontrolü, `assigned_to` sıfırlama, geçmiş tablosuna kayıt): 7 senaryo (yanlış birimin müdürü reddedilir, doğru birimin müdürü başarır, artık-yetkisi-kalmayan-müdür reddedilir, pasif hedef birim reddedilir, aynı birime yönlendirme reddedilir, `acik` olmayan durum reddedilir, admin birimden/durumdan bağımsız yetkili) gerçek iki müdür test hesabıyla tek tek doğrulandı; müdürün geçmiş tablosunu göremediği (admin-only RLS) ayrıca teyit edildi.
  - **Adım 6** — `request_detail_screen.dart`'a "Birim Değiştir" aksiyonu (müdür/admin, sadece `status='acik'` iken görünür; mevcut "Ata" aksiyonuyla birebir aynı dialog/RPC-çağırma deseni).
  - `flutter analyze` her adımda temiz; tüm test hesapları/talepleri temizlendi, veri sağlığı (2 birim/15 talep/6 kullanıcı/0 yönlendirme geçmişi) korundu.
  - **Test durumu:** Backend/mantık seviyesinde (curl, psql) kapsamlı doğrulandı. Gerçek Flutter UI'da (emülatör/cihaz) HENÜZ test edilmedi.
- **2026-07-21 (Faz 4 kapanışı):** Faz 4 gerçek cihazda test edildi. Test sırasında `admin_department_screen.dart`'ta bir hata bulundu ve düzeltildi: birim adı değiştirilince ekranda `'_dependents.isEmpty': is not true` assertion hatası çıkıyordu (işlem veritabanında başarıyla gerçekleşiyordu, sorun sadece UI'daydı). Kök neden: liste `.order('name')` ile alfabetik geldiği için bir birimin adını değiştirmek listede yer değiştirmesine yol açıyor, ama `ListView.builder` içindeki `ListTile`'ların hiç `key`'i yoktu — Flutter öğeleri index'e göre eşleştirdiğinden, bu yer değişimi rename dialogunun kapanma anıyla çakışınca Element ağacında tutarsızlığa yol açıyordu. Her `ListTile`'a `key: ValueKey(department['id'])` eklenerek düzeltildi (öğeler artık kimliğe göre takip ediliyor). Bu düzeltme, Faz 4'ün geri kalanıyla birlikte tek commit'te (`19bacb6`) birleştirildi.
- **2026-07-21 (Faz 5 — kapsam netleştirmesi):** Kullanıcı "Faz 5" adı altında talep oluşturma/listeleme/atama/çözümleme/onay akışının YENİDEN, model sınıfı + service katmanı + `StreamBuilder` mimarisiyle inşa edilmesini istedi. Bu, roadmap madde 5'te (2026-07-16) zaten tamamlanmış, çalışan bir özellikle (`request_create_screen.dart`/`request_list_screen.dart`/`my_requests_screen.dart`/`request_detail_screen.dart`, `Map<String,dynamic>` + doğrudan Supabase çağrısı mimarisiyle) karıştırılmıştı; istenen mimari de projede hiç kullanılmıyor (model class/service layer/realtime stream yok). Bu durum kullanıcıya açıkça belirtilip `AskUserQuestion` ile netleştirildi — kullanıcı gerçek Faz 5'i (CLAUDE.md'nin roadmap'indeki "SLA + istatistikler") istediğini onayladı, mevcut mimariyle tutarlı şekilde planlandı.
- **2026-07-21 (Faz 5):** Faz 5 planlandı ve kodlandı. Plan modunda keşif + bir Plan agent doğrulaması yapıldı; önemli bulgular: (1) müdürün `mudur_onay_verebilir` RLS politikası `approval_status`'un mevcut/yeni değerine hiç kısıtlama koymuyor — yani onaylı bir raporun UI dışından geri çevrilmesi DB seviyesinde engellenmiyor (UI hiç yapmıyor ama izin veriyor), bu yüzden `resolved_at` savunma amaçlı, onay geri alınırsa temizlenecek şekilde yazıldı; (2) migration dosyası append-only kronolojik bir günlük olduğu için (`handle_new_user()` emsali), `sync_request_status_from_result()` orijinal tanımına dokunulmadan dosyanın sonuna yeniden tanımlandı; (3) `OLD` kaydına INSERT olaylarında doğrudan SQL CASE içinde referans verilemeyeceği (runtime hatası) fark edilip, temizleme kararı PL/pgSQL `IF`/`TG_OP` kontrolüyle güvenli hale getirildi. Kullanıcıyla dört karar netleşti: SLA kontrolü DB-scheduled (pg_cron, bu kurulumda etkin değil) YERİNE uygulama açılışında; eşik sabit 3 gün; tek seferlik uyarı (tekrar hatırlatma yok); müdürsüz birimde admin'e bildirilir.
  - **Adım 1** — `requests.resolved_at`/`sla_notified_at` + `sync_request_status_from_result()` güncellemesi: 4 geçişli bir trigger testiyle (beklemede→cozuldu, onaylandi→resolved_at dolar, onaylandi→reddedildi→resolved_at temizlenir, tekrar onaylandi→yeniden dolar) doğrulandı. `request_detail_screen.dart`'a "Çözüm Süresi" satırı eklendi (intl paketi yok, notifications_screen.dart'taki hand-rolled Türkçe formatlama deseniyle).
  - **Adım 2** — `check_sla_breaches()` RPC + `home_screen.dart` (`_isMudur`, fire-and-forget tetikleme): 4 test talebiyle (aktif müdürlü birim, müdürsüz birim, yeni talep, zaten çözülmüş talep) doğrulandı — doğru kişiye doğru mesaj gitti, `sla_notified_at` sadece gerçek bildirim gidince set edildi, ikinci çağrıda mükerrer bildirim gitmedi, pasif kullanıcı reddedildi.
  - **Adım 3** — `get_admin_stats()`/`get_manager_stats()` RPC'leri + `admin_stats_screen.dart`/`manager_stats_screen.dart` + "İstatistikler" butonu: gerçek verilerle (3 birim, 15 talep) birim×durum kırılımının doğru sayıldığı, müdürün sadece kendi biriminin sayılarını gördüğü, çapraz yetki denemelerinin (müdür→admin RPC'si, admin→müdür RPC'si) reddedildiği doğrulandı.
  - `flutter analyze` her adımda temiz; tüm test hesapları/talepleri/bildirimleri temizlendi. Not: kullanıcının kendi Faz 4 testi sırasında oluşturduğu üçüncü bir birim ("üçüncü birim") fark edildi, dokunulmadı — yeni referans veri sağlığı: 3 birim/15 talep/6 kullanıcı/6 sonuç.
  - **Test durumu:** Backend/mantık seviyesinde kapsamlı doğrulandı. Gerçek Flutter UI'da HENÜZ test edilmedi.
- **2026-07-17 (özet):** Roadmap madde 6 (Medya ekleri) tamamlandı ve emülatörde uçtan uca test edildi — talep oluştururken foto/video/belge seçme ve yükleme, talep detayında ekleri görüntüleme (fotoğraf önizleme + tam ekran, video/belge ikon+ad). Aynı oturumda ayrıca: "Gönder" butonuna mükerrer tıklamayla aynı talebin birden fazla kez oluşturulması engellendi, başarılı gönderimde giriş yapmış kullanıcılar için SnackBar + "Taleplerim"e yönlendirme eklendi, müdürün talep açıklamasını görememesi şikâyeti araştırıldı (kodda hata bulunamadı, eski build sorunu olduğu doğrulandı).
- **2026-07-22 (Beni Hatırla / oturum akışı yeniden kuruldu):** Login ekranına "Beni Hatırla" checkbox'ı eklendi. YENİ `lib/services/local_prefs_service.dart` (`shared_preferences` ile `remember_me` + `cached_full_name` saklıyor — bu değerler HİÇBİR ZAMAN güvenlik kararı için kullanılmıyor, sadece açılışta hangi ekranın gösterileceğine karar veriyor, gerçek oturum geçerliliği her zaman sunucuya soruluyor). `main.dart`'taki `AuthGate`, `StatefulWidget`'a çevrilip üç kollu karar veriyor: `remember_me=false` → varsa mevcut oturum zorla `signOut()` edilip boş giriş formu; `remember_me=true` + yerelde oturum var → YENİ `welcome_back_screen.dart` (şifre sormadan "hoş geldin" ekranı — "Giriş Yap" butonu, `users` tablosuna gerçek bir sorgu atarak oturumu sessizce doğruluyor, bu sorgu hem geçersiz/süresi dolmuş token'ı hem pasifleştirilmiş hesabı aynı anda yakalıyor); `remember_me=true` + oturum yok → boş forma düşer. `pubspec.yaml`'a `shared_preferences` eklendi (zaten Supabase'in kendi oturum saklamasının transitive bağımlılığıymış).
- **2026-07-22 (admin_department_screen.dart — `_dependents.isEmpty`'nin GERÇEK kök nedeni bulundu):** Faz 4 kapanışında (2026-07-21) "düzeltildi" denen birim adı düzenleme hatasıyla kullanıcı gerçek cihazda tekrar karşılaştı — o düzeltme (ListTile'lara key eklemek) yanlış teşhise dayanıyormuş. Bu oturumda üç teori daha sırayla denenip gerçek cihazda (adb ile otomatik dokunma + ekran görüntüsü + logcat) test edildi (liste reorder'ının dialog kapanış animasyonuyla çakışması, `FocusScope.unfocus()`'un Navigator'la çakışması, `Navigator.of(dialogContext)`'in `await`'ten sonra çağrılması) — hiçbiri kalıcı çözmedi, ikisi bazen geçip bazen çökerek yanıltıcı "düzeldi" izlenimi bile verdi. Kesin teşhis, bu cihazda/derlemede Flutter hata raporlamasının logcat'e hiç düşmediği fark edilip `flutter attach` ile canlı VM service'e bağlanarak TAM stack trace yakalanınca bulundu: gerçek hata `_dependents.isEmpty` değil, `"A TextEditingController was used after being disposed"` imiş (kullanıcının Edge DevTools'ta gördüğü, önceden gözden kaçırılan ikinci hata mesajı doğruymuş). Kök neden: `_showEditDialog`'daki `TextEditingController`, `await showDialog(...)` döner dönmez (yani `Navigator.pop()` çağrıldığı ANDA, ama dialogun kapanış animasyonu hâlâ sürerken) elle `dispose()` ediliyordu. Kalıcı çözüm: `request_detail_screen.dart`'taki `_ResolveReportDialog`/`_EditRequestDialog` ile AYNI, kanıtlanmış deseni izleyerek dialogu kendi controller'ını kendi `State.dispose()`'unda temizleyen gerçek bir `StatefulWidget` (`_EditDepartmentDialog`) yapmak — controller'ın ömrü artık Future tamamlanma anına değil, Element'in gerçekten unmount olma anına (kapanış animasyonu bitene kadar) bağlı. Cihazda 5 ardışık düzenle→kaydet denemesiyle (temiz süreç + gerçek isim değişikliğiyle) doğrulandı, logcat temiz. Bu teşhis yöntemi (`flutter attach`) "Bilinen Ortam Notları"na eklendi.
- **2026-07-22 (profile_screen.dart — admin/personel/müdürde "Profilim" butonu sessizce hata veriyordu):** Kök neden: `departments.id`/`users.department_id` veritabanında `bigint` (diğer tabloların aksine UUID DEĞİL), ama `profile_screen.dart`'ta `profile['department_id'] as String?` diye cast ediliyordu. Vatandaşta bu alan hep `null` olduğu için `null as String?` sorunsuz geçiyor (bu yüzden vatandaşta çalışıyor gibi görünüyordu), ama admin/personel/müdürde gerçek bir `int` (canlı DB'de doğrulandı: `3`/`4`) olduğu için `TypeError` fırlatıp ekranın genel `catch (_)` bloğunda sessizce yutuluyordu. Cast kaldırılıp `dynamic` bırakıldı — `request_list_screen.dart`/`request_detail_screen.dart`'ta zaten doğru kullanılan desenle aynı.
- **2026-07-22 (üç UI iyileştirmesi):** (1) `login_screen.dart`/`register_screen.dart`/`personnel_register_screen.dart`'taki şifre alanlarına göster/gizle ikonu eklendi (`_obscurePassword` state + `suffixIcon`). (2) `home_screen.dart`, `PopScope(canPop: false)` ile sarmalandı — ana ekran girişten sonra `pushReplacement` ile Navigator'daki tek route olduğu için Android geri tuşu doğrudan uygulamadan çıkışa denk geliyordu; artık önce onay dialogu çıkıyor, "Evet" denirse `SystemNavigator.pop()` çağrılıyor. (3) YENİ `lib/widgets/status_badge.dart` — `StatusBadge` widget'ı, talep durumunu (açık: turuncu, çözüldü: mavi, onaylandı: yeşil, reddedildi: kırmızı, iptal: gri) `request_list_screen.dart`'ın zaten kullandığı Chip görsel diliyle (shade900/shade100) rozet olarak gösteriyor; `request_list_screen.dart`/`my_requests_screen.dart`/`query_result_screen.dart`/`request_detail_screen.dart`'a entegre edildi, üç dosyadaki tekrarlı yerel `_statusLabels` map'leri kaldırılıp paylaşılan `request_filters.dart`'taki `statusLabels`'a yönlendirildi.
- **2026-07-22 (İstatistik ekranları gerçek dashboard'a dönüştürüldü):** `admin_stats_screen.dart`/`manager_stats_screen.dart`, düz metin listelerinden `fl_chart` (^1.2.0) tabanlı bir dashboard'a dönüştürüldü: KPI kart grid'i (Toplam + 4 durum kartı, `status_badge.dart`'tan paylaşılan renk paletiyle — bu yüzden `StatusBadge`'in özel renk map'i public `statusColors`'a çıkarıldı), sadece admin'de birim×durum gruplu çubuk grafiği (müdürde tek birim olduğu için kaldırıldı, zaten pasta grafiği aynı bilgiyi durum kırılımıyla veriyor), genel durum dağılımı donut grafiği (`StatusPieChart`, ikisinde de), aylık ortalama çözüm süresi çizgi grafiği (`ResolutionTrendChart`). YENİ paylaşılan `lib/widgets/stats_dashboard_widgets.dart` (`KpiCard`/`StatusPieChart`/`ResolutionTrendChart`). Çizgi grafik için YENİ `get_admin_resolution_trend()`/`get_manager_resolution_trend()` RPC'leri yazıldı — `get_admin_stats()`/`get_manager_stats()` ile AYNI rol-ayrımı deseni (tek RPC + role'e göre dönüş şekli değiştirme YERİNE iki ayrı RPC), aylık kırılım (`date_trunc('month', ...)`), `resolved_at is not null` filtresiyle geçmişe dönük boş kayıtlar hariç tutuluyor, grafikte dönen en eski dönemden türetilen "X tarihinden itibaren veri mevcuttur" notu var (sabit kodlanmış bir tarih DEĞİL). Canlı DB'de doğrulandı (şu an 1 veri noktası: Temmuz 2026, ort. ~114.75 saat — CLAUDE.md'nin bilinen "resolved_at verisi az" sınırlamasıyla tutarlı). `flutter analyze` temiz. **Test durumu:** Backend/mantık + statik analiz seviyesinde doğrulandı; gerçek cihazda admin/müdür rolleriyle görsel/etkileşim testi HENÜZ yapılmadı.

## Yapılacaklar / Production Öncesi Kontrol Listesi
- [ ] `GOTRUE_MAILER_AUTOCONFIRM=true` (docker-compose.yml / ilgili `.env`) sadece YEREL GELİŞTİRME ayarıdır — e-posta doğrulaması olmadan hesapları otomatik onaylıyor. Production'a geçmeden önce gerçek bir SMTP servisi bağlanıp bu ayar `false` yapılmalı; aksi halde gerçek kullanıcılar e-posta doğrulaması yapmadan hesap açabilir (bkz. 2026-07-13 tarihli İlerleme Günlüğü kayıtları). **Durum (2026-07-16): henüz değiştirilmedi, sadece kayıt altına alındı.**
- [ ] Ek (attachment) silme özelliği MVP'de yok, ileride eklenebilir (2026-07-17: `attachments` tablosu/bucket için bilinçli olarak DELETE RLS politikası yazılmadı — admin dahil hiç kimse şu an bir eki silemiyor).
- [ ] Anonim (access_token ile takip edilen) taleplerde medya eki görüntüleme/yükleme şu an desteklenmiyor — `created_by` null olduğu için RLS eşleşmiyor; ileride token bazlı ayrı bir erişim yolu tasarlanmalı.
- [ ] KVKK aydınlatma metni + açık rıza kutusu + `kvkk_accepted_at` kaydı, uygulama gerçek kullanıcılara açılmadan önce MUTLAKA eklenecek (T.C. kimlik no toplandığı için yasal zorunluluk). (2026-07-20: Faz 1 kapsamından bilinçli olarak çıkarıldı, kayıt ekranlarında şu an KVKK onayı alınmıyor.)

## Bilinen Ortam Notları
- **Bu makinedeki `curl`, `--data-urlencode` ile Türkçe karakterleri hatalı encode ediyor (2026-07-21):** Windows/mingw derlemesi (`curl 8.21.0 (x86_64-w64-mingw32)`), `--data-urlencode` ile gönderilen değerlerde Türkçe karakter (ör. `ö`, `ğ`) varsa PostgREST'ten sahte bir `500 Something went wrong` dönüyor — bu backend'de veya Flutter/Dart tarafında GERÇEK bir hata DEĞİL, sadece bu curl derlemesinin bir kusuru (bayt düzeyinde doğru UTF-8 gönderdiği doğrulandı — `xxd` ile kontrol edildi — ama Kong/PostgREST'e ulaşana kadar bir yerde bozuluyor). **Kural:** Bu makinede Türkçe karakter içeren bir sorgu parametresini curl ile test ederken `--data-urlencode` yerine, değeri elle (veya bir Dart scriptiyle `Uri.encodeQueryComponent` kullanılarak) percent-encode edip URL'e doğrudan gömün — Faz 3'teki `.or()` escape testinde bu yöntemle sorun tamamen ortadan kalktı. Swagger UI veya gerçek Flutter uygulaması (kendi HTTP client'ı düzgün UTF-8 encode ettiği için) bu sorunu hiç YAŞAMAZ — sadece bu spesifik curl kullanım şekli etkileniyor.
- **GoTrue /signup endpoint'i, trigger'dan gelen özel hataları sansürlüyor (2026-07-20, önemli teknik ders):** `gotrue` Dart paketi (dolayısıyla `supabase_flutter`), her `/signup` isteğine otomatik olarak `X-Supabase-Api-Version: 2024-01-01` header'ı ekliyor. Bu header, sunucudaki (self-hosted) GoTrue'yu, `handle_new_user()` trigger'ı içinde `RAISE EXCEPTION` ile fırlatılan HER özel Postgres hatasını (format hatası, benzersizlik ihlali, eksik alan, geçersiz davet kodu — hepsi) generic bir mesaja sarmalamaya zorluyor: `{"code":"unexpected_failure","message":"Database error saving new user"}`. Bu, paket seviyesinde sabit bir davranış — bizim tarafımızdan kapatılamaz veya bypass edilemez. Bu header'sız yapılan curl/Swagger testleri bu sansürü GÖRMEZ (o yüzden 2026-07-13/14 tarihli "test edildi, doğrulandı" notları yanıltıcıydı — gerçek uygulama davranışını yansıtmıyorlardı).
  - **Kural:** Kullanıcıya özel/anlaşılır bir hata mesajı göstermemiz gereken TÜM kayıt-zamanı doğrulamalar (tc_no format/benzersizlik, davet kodu geçerliliği, vb.), `signUp()`'a GÜVENİLEREK trigger içinde yapılamaz. Bunun yerine `signUp()`'tan ÖNCE, ayrı bir `security definer` RPC ile (Postgrest üzerinden, GoTrue'nun `/signup` sanitizasyonuna hiç girmeden) kontrol edilmeli — RPC'den dönen `PostgrestException.message` güvenilir şekilde gerçek Türkçe metni taşır. Bkz. `check_registration_availability()` RPC'si ve `AuthService.checkRegistrationAvailability()`.
  - Trigger'daki kontroller yine de KALMALI — nihai güvenlik ağı (yarış durumu ihtimaline karşı); sadece kullanıcıya mesaj göstermek için trigger'ın hatasına güvenilmiyor.
- **Gerçek Android cihazda (USB) test etmeden önce (2026-07-20):** `supabase_service.dart`, `device_info_plus` ile emülatör/gerçek cihaz ayrımı yapıyor: emülatörde host'a ulaşmak için sabit `10.0.2.2` kullanılıyor, gerçek cihazda ise URL `localhost` olarak bırakılıyor — bunun çalışması için geliştiricinin cihazı USB ile bağlayıp **`adb reverse tcp:8000 tcp:8000`** komutunu çalıştırmış olması ZORUNLU (cihazın `localhost:8000`'ini host makinenin `localhost:8000`'ine yönlendirir). Bu komut adb sunucusu/cihaz bağlantısı her sıfırlandığında (USB çıkar-tak, `adb kill-server` vb.) tekrar çalıştırılmalı — kalıcı bir sistem ayarı değil, oturum bazlı bir yönlendirme kuralıdır.
- **Android emülatöründe fiziksel klavye Türkçe karakter sorunu (2026-07-17):** Windows host klavyesinden emülatöre Türkçe karakterler (ş, ı, ğ vb.) doğru iletilmiyor. Bu bir uygulama/kod hatası DEĞİL, saf bir emülatör/host fiziksel klavye eşleme sorunu — doğrulandı çünkü emülatörün kendi dokunmatik ekran klavyesine geçilince Türkçe karakterler sorunsuz giriliyor. **Çözüm:** Emülatörde Türkçe karakter girilmesi gereken testlerde fiziksel klavye yerine emülatörün dokunmatik ekran klavyesini kullanın.
- **Bu cihazda/derlemede Flutter'ın hata raporlaması logcat'e hiç düşmüyor (2026-07-22, önemli teşhis dersi):** Normalde Flutter'ın varsayılan `FlutterError.onError`'ı her widget hatasını `debugPrint` üzerinden Android logcat'in "flutter" etiketine yazar. Bu makine/cihaz/derleme kombinasyonunda (Samsung A56, gerçek cihaz, Impeller/Vulkan) bu HİÇ gerçekleşmedi — `adb logcat -s flutter:V` ile (hatanın oluştuğu ana kadar temiz tutulmuş bir tamponla bile) hiçbir çıktı yakalanamadı; ekranda kırmızı hata ekranı görünmesine rağmen. **Çözüm/kural:** Böyle bir durumda `flutter attach --debug-uri <VM service URL> -d <cihaz>` ile çalışan uygulamaya bağlanın — VM service'in konsol çıktısı (tam stack trace, "The relevant error-causing widget was" dahil) doğrudan terminale basılır, logcat'e hiç ihtiyaç kalmaz. VM service URL'i uygulama ilk açıldığında logcat'te ("Dart VM service is listening on http://127.0.0.1:PORT/TOKEN=/") bir kereliğine görünür, `adb forward tcp:PORT tcp:PORT` ile host'a yönlendirilebilir. Bu yöntem olmadan (sadece ekrandaki kısaltılmış assertion metnine bakarak) `admin_department_screen.dart`'taki gerçek hata (bkz. İlerleme Günlüğü, 2026-07-22) üç ayrı yanlış teşhise yol açmıştı.

## Bilinen Eksikler / Sonraki Adımlar
- `request_department_reassignments` geçmişi şu an sadece admin'e görünür (RLS), müdürlere görünürlük bilinçli olarak bu fazın kapsamı dışında bırakıldı (kullanıcı kararı) — UI'da da hiç tüketilmiyor, ileride bir istatistik/denetim ekranı ile birlikte ele alınabilir.
- Admin'in kendi kendini pasifleştirmesi RPC seviyesinde engellendi, ama başka bir adminin onu pasifleştirmesi hâlâ mümkün — sistemde en az bir aktif admin kalması garanti edilmiyor (uç durum, kullanıcı sayısı arttıkça gözden geçirilebilir).
- Birimler için hâlâ gerçek bir silme (hard delete) yolu yok, sadece pasifleştirme — bu bilinçli bir tasarım kararı (geçmiş taleplerin birim referansı bozulmasın diye), ama birim listesinin zamanla sadece büyüyeceği anlamına geliyor.
- Şifremi unuttum e-postasındaki LİNK bilinçli olarak kullanılmıyor (bkz. Faz 2 notları, "link neden kullanılmıyor") — bunun yerine 6 haneli kod + `reset_password_screen.dart` kalıcı çözüm olarak uygulandı, deep link (app_links paketi) hiç gerekmedi. Bu madde artık AÇIK DEĞİL, kapatıldı.
- Profil ekranında e-posta değişikliği Faz 2'den bilinçli olarak çıkarıldı (Supabase'in e-posta onay akışı + `public.users.email`/`auth.users.email` senkron sorunu nedeniyle) — ileride ayrı bir "e-posta değiştir" akışı olarak ele alınabilir.
- Yerel Supabase kurulumunda Inbucket'ın kalıcı olarak devrede kalması için `C:\Users\makto\supabase\docker\.env`'deki `COMPOSE_FILE` hâlâ sadece `docker-compose.yml`'e işaret ediyor — `mail` container'ı şu an elle başlatıldığı haliyle çalışıyor ama makine yeniden başlatılırsa/`docker compose down` sonrası tekrar elle (`-f dev/docker-compose.dev.yml`, sadece `mail`/`auth` servisleri hedeflenerek, `db`'nin riskli volume override'ından kaçınarak) başlatılması gerekir.
- `get_request_by_token` fonksiyonunda "kayıt bulunamadı" ile "yetkisiz erişim" (`requester_type = 'personel'` olan bir talebe token ile erişilmeye çalışılması) durumlarını ayrı mesajlarla ayırt etme fikri bilinçli olarak ertelendi. Güvenlik zaten sağlanmış durumda (fonksiyon her iki durumda da boş liste döndürüyor, personel taleplerine token ile erişim mümkün değil); bu yalnızca bir UX inceliği, ileride ele alınabilir.
- İstatistik dashboard'u (2026-07-22, `fl_chart`) ve "Beni Hatırla" oturum akışı, backend/mantık + statik analiz seviyesinde doğrulandı ama gerçek cihazda (admin VE müdür rolleriyle ayrı ayrı, ikisinin farklı veri kapsamı gördüğü göz önünde bulundurularak) HENÜZ test edilmedi — sıradaki adım.
